# 04-序列建模：HMM与CRF

> **本文定位**：开卷考试中"序列模型"相关题目的核心参考。涵盖HMM三个基本问题、前向/后向/Viterbi算法、Baum-Welch(EM)、CRF、Max-Plus代数视角。
> **老师偏好**：HMM前向/Viterbi手算、Baum-Welch E步计算、HMM vs CRF辨析。

---

## 目录

- [一、序列标注问题概述](#一序列标注问题概述)
- [二、HMM基础](#二hmm基础)
- [三、前向算法（Forward Algorithm）](#三前向算法forward-algorithm)
- [四、后向算法（Backward Algorithm）](#四后向算法backward-algorithm)
- [五、Viterbi算法（解码）](#五viterbi算法解码)
- [六、Baum-Welch算法（EM学习）](#六baum-welch算法em学习)
- [七、BIO分词完整示例](#七bio分词完整示例)
- [八、条件随机场（CRF）](#八条件随机场crf)
- [九、Max-Plus代数视角](#九max-plus代数视角)
- [附录A：公式速查表](#附录a公式速查表)
- [附录B：常见考试题型](#附录b常见考试题型)

---

## 一、序列标注问题概述

### 1.1 什么是序列标注

> **直观理解**：给一个序列中的每个元素打标签。比如"我喜欢学习"四个字，标注为"B(词首) I(词中) O(非词) O(非词)"就是分词。HMM和CRF是两种解决这类问题的经典模型。

### 1.2 生成模型 vs 判别模型

| | 生成模型（HMM） | 判别模型（CRF） |
|---|---|---|
| **建模什么** | 联合概率 P(Y, X) | 条件概率 P(Y\|X) |
| **核心思想** | "这个世界如何生成数据" | "看到数据后如何判断标签" |
| **特征** | 只能用预设的特征（转移+发射） | 可以用**任意特征** |
| **标签偏置** | 有（Label Bias Problem） | 无（全局归一化） |

> **直观理解**：HMM花精力去建模"字是怎么来的"（P(字|标签)），CRF只关心"给标签打分"。

---

## 二、HMM基础

### 2.1 HMM五元组

| 符号 | 名称 | 含义 |
|------|------|------|
| **S** | 隐状态集 | {s₁, s₂, ..., sₙ}（如BIO标签） |
| **K** | 观测集 | {o₁, o₂, ..., oₘ}（如所有汉字） |
| **Π** | 初始状态分布 | πᵢ = P(s₁=i) |
| **A** | 状态转移矩阵 | aᵢⱼ = P(sₜ₊₁=j \| sₜ=i) |
| **B** | 发射概率矩阵 | bⱼ(o) = P(oₜ=o \| sₜ=j) |

### 2.2 两个基本假设

1. **一阶马尔可夫假设**：当前状态只依赖于前一个状态
2. **观测独立假设**：当前观测只依赖于当前状态

> **注意**：CRF放松了假设2（观测独立）——CRF的特征可以看全局上下文。

### 2.3 三个基本问题

| 问题 | 名称 | 输入 | 输出 | 算法 |
|------|------|------|------|------|
| **Q1: 评估** | Likelihood | λ=(Π,A,B), O | P(O\|λ) | 前向/后向 |
| **Q2: 解码** | Decoding | λ, O | 最优状态序列 S* | Viterbi |
| **Q3: 学习** | Learning | O | λ的最佳估计 | Baum-Welch (EM) |

> **直观理解**：评估="这个模型给这段观测打多少分"，解码="这段观测最可能对应什么隐藏序列"，学习="只有观测数据，怎么学出模型参数"。

---

## 三、前向算法（Forward Algorithm）

### 3.1 核心思想

> **直观理解**：直接枚举所有可能的状态序列是 O(Nᵀ)，指数爆炸。前向算法用**动态规划**避免重复计算——αₜ(i)存储"到时刻t且状态为i时，已看到的前t个观测的联合概率"。

**定义**：
$$\alpha_t(i) = P(o_1, o_2, ..., o_t, s_t = i | \lambda)$$

### 3.2 指令级手算步骤

| Step | 公式 | 说明 |
|------|------|------|
| **初始化** | $\alpha_1(i) = \pi_i \cdot b_i(o_1)$ | 时刻1：初始概率×发射概率 |
| **递推** | $\alpha_t(j) = \left[\sum_{i=1}^N \alpha_{t-1}(i) \cdot a_{ij}\right] \cdot b_j(o_t)$ | 对所有可能的上一状态i求和 |
| **终止** | $P(O|\lambda) = \sum_{i=1}^N \alpha_T(i)$ | 最终概率=所有终态α之和 |

### 3.3 手算例题（必练）

**给定一个简单的2状态HMM**：
- Π = [0.6, 0.4]（初始：状态1概率0.6，状态2概率0.4）
- A = [[0.7, 0.3], [0.4, 0.6]]（转移矩阵）
- B = [[0.5, 0.5], [0.1, 0.9]]（B[状态][观测]：状态1发射o1/o2各0.5，状态2偏o2）
- 观测序列：O = [o₁, o₂]

**前向计算**：

| Step | 计算 | 结果 |
|------|------|------|
| **t=1** | | |
| α₁(1) = π₁·b₁(o₁) | 0.6 × 0.5 | **0.30** |
| α₁(2) = π₂·b₂(o₁) | 0.4 × 0.1 | **0.04** |
| **t=2** | | |
| α₂(1) = [α₁(1)·a₁₁ + α₁(2)·a₂₁] · b₁(o₂) | [0.30×0.7 + 0.04×0.4] × 0.5 = [0.21+0.016] × 0.5 | **0.113** |
| α₂(2) = [α₁(1)·a₁₂ + α₁(2)·a₂₂] · b₂(o₂) | [0.30×0.3 + 0.04×0.6] × 0.9 = [0.09+0.024] × 0.9 | **0.103** |
| **终止** | | |
| P(O) = α₂(1) + α₂(2) | 0.113 + 0.103 | **0.216** |

**手算表格模板**（开卷考试直接用）：

| t | 状态 | αₜ(i) | 计算公式 |
|---|------|-------|---------|
| 1 | 1 | 0.30 | π₁×b₁(o₁) |
| 1 | 2 | 0.04 | π₂×b₂(o₁) |
| 2 | 1 | 0.113 | [0.30×0.7+0.04×0.4]×0.5 |
| 2 | 2 | 0.103 | [0.30×0.3+0.04×0.6]×0.9 |

---

## 四、后向算法（Backward Algorithm）

### 4.1 定义

$$\beta_t(i) = P(o_{t+1}, o_{t+2}, ..., o_T | s_t = i, \lambda)$$

> **直观理解**：后向概率是从"当前状态i"出发，之后能观测到 oₜ₊₁...o_T 的概率。"已知我现在在状态i，未来的观测路径有多可能"。

### 4.2 指令级步骤

| Step | 公式 | 说明 |
|------|------|------|
| **初始化** | $\beta_T(i) = 1$ | 终态后向概率恒为1 |
| **递推** | $\beta_t(i) = \sum_{j=1}^N a_{ij} \cdot b_j(o_{t+1}) \cdot \beta_{t+1}(j)$ | 对下一状态j求和 |
| **终止** | $P(O|\lambda) = \sum_{i=1}^N \pi_i \cdot b_i(o_1) \cdot \beta_1(i)$ | 验证=前向结果 |

### 4.3 与前向算法的互补性

| | 前向 α | 后向 β |
|---|---|---|
| 时刻方向 | 1→T | T→1 |
| 含义 | "到现在为止" | "从今往后" |
| 联合 | γₜ(i)=αₜ(i)βₜ(i)/P(O) = 完整后验 | — |

---

## 五、Viterbi算法（解码）

### 5.1 与Forward的区别

| | Forward | Viterbi |
|---|---|---|
| **聚合方式** | **sum**（所有路径加权和） | **max**（只留最优路径） |
| **用途** | 计算P(O\|λ) | 找最优状态序列 |
| **回溯** | 不需要 | **需要**（ψ指针） |

### 5.2 指令级手算步骤

| Step | 公式 | 说明 |
|------|------|------|
| **初始化** | δ₁(i) = πᵢ × bᵢ(o₁) | 同Forward开始 |
| | ψ₁(i) = 0 | 无回溯（时刻1没有前驱） |
| **递推** | δₜ(j) = **maxᵢ**[δₜ₋₁(i) × aᵢⱼ] × bⱼ(oₜ) | 取max而非sum！ |
| | ψₜ(j) = **argmaxᵢ**[δₜ₋₁(i) × aᵢⱼ] | 记录最佳前驱 |
| **终止** | P* = maxᵢ δ_T(i) | 最优路径得分 |
| | s*_T = argmaxᵢ δ_T(i) | 最优终态 |
| **回溯** | s*ₜ₋₁ = ψₜ(s*ₜ) | 从T倒推到1 |

### 5.3 手算例题（同上HMM·必练）

**同一HMM**：Π=[0.6,0.4], A=[[0.7,0.3],[0.4,0.6]], B=[[0.5,0.5],[0.1,0.9]], O=[o₁,o₂]

| Step | 计算 | δ | ψ |
|------|------|----|----|
| **t=1** | | |
| δ₁(1) = π₁·b₁(o₁) | 0.6×0.5=**0.30** | 0.30 | 0 |
| δ₁(2) = π₂·b₂(o₁) | 0.4×0.1=**0.04** | 0.04 | 0 |
| **t=2** | | |
| δ₂(1) | max(0.30×0.7, 0.04×0.4) × 0.5 = max(0.21, 0.016)×0.5 | **0.105** | ψ₂(1)=**1**（来自状态1） |
| δ₂(2) | max(0.30×0.3, 0.04×0.6) × 0.9 = max(0.09, 0.024)×0.9 | **0.081** | ψ₂(2)=**1**（来自状态1） |
| **终止** | | |
| 最优 | max(0.105, 0.081)=0.105 | s*₂=**1** | |
| **回溯** | ψ₂(1)=1 | s*₁=**1** | |

**最优状态序列**：s* = [**1, 1**]

---

## 六、Baum-Welch算法（EM学习）

### 6.1 EM的核心思想

> **直观理解**：不知道标签 → 用当前模型"猜"每个时刻属于每个状态的概率（E步） → 用这些"软计数"重新估计模型参数（M步） → 迭代直到收敛。这就是"无监督HMM训练"。

**EM = 软Viterbi训练**：
- Viterbi：每个时刻硬性指派给一个最优状态
- EM：每个时刻保留对所有状态的概率分布（软分配）

### 6.2 E步：计算γ和ξ（手算重点）

**关键符号**：
- γₜ(i) = P(sₜ=i | O) ——时刻t处于状态i的后验概率
- ξₜ(i,j) = P(sₜ=i, sₜ₊₁=j | O) ——相邻时刻的状态对后验概率

**γₜ(i)计算**（指令级）：

$$\gamma_t(i) = \frac{\alpha_t(i) \cdot \beta_t(i)}{P(O)} = \frac{\alpha_t(i) \cdot \beta_t(i)}{\sum_k \alpha_T(k)}$$

| Step | 操作 |
|------|------|
| 1 | 计算前向α（Forward）、后向β（Backward） |
| 2 | P(O) = Σₖ α_T(k) |
| 3 | 对每个(t,i)：γₜ(i) = αₜ(i)×βₜ(i) / P(O) |

**ξₜ(i,j)计算**：

$$\xi_t(i,j) = \frac{\alpha_t(i) \cdot a_{ij} \cdot b_j(o_{t+1}) \cdot \beta_{t+1}(j)}{P(O)}$$

> **记忆口诀**：ξ = α(i) × (转移i→j) × (发射j→oₜ₊₁) × β_{t+1}(j) / P(O)。一条"经过i→j这条边"的完整路径的概率。

### 6.3 M步：参数重估（把软计数当硬计数）

| 参数 | 公式 | 含义 |
|------|------|------|
| **初始概率** | $\hat{\pi}_i = \gamma_1(i)$ | 时刻1在状态i的概率 |
| **转移概率** | $\hat{a}_{ij} = \frac{\sum_{t=1}^{T-1} \xi_t(i,j)}{\sum_{t=1}^{T-1} \gamma_t(i)}$ | 从i到j的次数 / 从i出发的次数 |
| **发射概率** | $\hat{b}_j(v) = \frac{\sum_{t=1}^{T} \gamma_t(j) \cdot \mathbb{1}(o_t = v)}{\sum_{t=1}^{T} \gamma_t(j)}$ | 状态j中观测v的次数 / 状态j的出现次数 |

> **关键理解**：分母的Σγₜ(i)不是整数（是"软计数"=期望次数），这就是EM与MLE的根本区别。

### 6.4 γ和ξ的性质（验证）

$$\sum_i \gamma_t(i) = 1 \quad \text{（每个时刻概率归一化）}$$
$$\sum_i \sum_j \xi_t(i,j) = 1 \quad \text{（每个时刻的转移总和为1）}$$
$$\gamma_t(i) = \sum_j \xi_t(i,j)$$

---

## 七、BIO分词完整示例

### 7.1 场景

> 句子："我喜欢学习"（4个字），用HMM做BIO标注（B=词首,I=词中,O=非词）。EM算法无监督学习。

### 7.2 E步输出示例

假设当前参数下：
- γ₂(B)=0.7, γ₂(I)=0.3（"喜"字：70%概率是词首，30%是词中）
- ξ₂(B, I)=0.5（"喜"→"欢"以B→I转移的概率）

### 7.3 M步更新示例

| 参数 | 更新 |
|------|------|
| b_B("喜") | 计数+0.7（"喜"以0.7概率属于B） |
| b_I("喜") | 计数+0.3 |
| a_{B→I} | 计数+0.5 |

### 7.4 最终学到的模式

- **转移**：B→I概率大（词首后接词中）、I→B/O概率大（词中后要么继续I，要么结束→下一个词首B）
- **发射**："的"常为O，"北京"中"北"常为B、"京"常为I

---

## 八、条件随机场（CRF）

### 8.1 CRF定义

$$P(y|x) = \frac{1}{Z(x)} \exp\left(\sum_j \lambda_j F_j(y, x)\right)$$

| 符号 | 含义 |
|------|------|
| Fⱼ(y,x) | 特征函数（如"标签为B且上一个标签为O时计数"） |
| λⱼ | 特征权重（学习的参数） |
| Z(x) | 配分函数（归一化常数） |
| Score = ΣλⱼFⱼ | 路径得分（未归一化） |

### 8.2 特征模板

| 类型 | 例子 | 含义 |
|------|------|------|
| **一元特征** | "当前字='学'&标签=B" | 发射特征 |
| **二元特征** | "前标签=O&当前标签=B" | 转移特征 |
| **组合特征** | "当前字='学'&前字='欢'&标签=I" | 任意组合！ |

> **HMM做不到的**：HMM只能用P(字|标签)这种固定形式。CRF可以把"前一个字+当前字+标签"这种复杂模式直接当特征。

### 8.3 HMM vs CRF（核心对比·必考）

| 维度 | HMM | CRF |
|------|-----|-----|
| **建模方式** | 生成式（P(X,Y)） | 判别式（P(Y\|X)） |
| **特征** | 仅转移+发射 | **任意定义** |
| **归一化** | 局部（每步） | **全局**（Z(x)对所有路径） |
| **标签偏置** | **有** | 无 |
| **训练** | 简单（计数） | 需前向-后向+梯度 |
| **观测独立假设** | 严格 | 不要求 |

### 8.4 标签偏置问题（Label Bias Problem）

> **直观理解**：HMM在每个时刻必须决定"往哪走"——状态出度小的节点会"被迫"走某条路。即使那条路整体不好，因为局部被迫选择，HMM也会偏向它。CRF在所有时刻一起归一化，所以没这个问题。

**示例**：状态1只有一个出边(→状态2)。HMM中，时刻t在状态1时必须100%走状态2，即使状态2的后续观测概率很低。CRF中，所有路径一起比较，不会被迫。

### 8.5 CRF+NN

现代做法：CRF的发射得分由神经网络计算（如BiLSTM编码每个字→发射得分），转移得分仍然是CRF的参数。

$$Score(y) = \sum_t (A_{y_{t-1},y_t} + f_\theta(y_t|t))$$

- $A$：转移矩阵（CRF参数）
- $f_\theta$：神经网络对时刻t的输出（发射得分）

---

## 九、Max-Plus代数视角

### 9.1 什么是Max-Plus代数

> **定义**：一种代数系统，其中 ⊕ = max（"加法"），⊗ = +（"乘法"）。

| | 普通代数 | Max-Plus代数 |
|---|---|---|
| 加法 | a+b | a⊕b = max(a,b) |
| 乘法 | a×b | a⊗b = a+b |
| 零元 | 0 | −∞ |
| 幺元 | 1 | 0 |

### 9.2 CRF = 加权有向图上的最长路径

> **核心洞察**：CRF的链式结构 = 一个加权有向图。节点=标签×时刻（权重=发射得分），边=转移（权重=转移得分）。路径得分 = 所有节点权重+所有边权重 = Max-Plus中的"总乘积"。

### 9.3 Max-Plus矩阵乘法

$$(A \otimes B)_{i,j} = \max_k (A_{i,k} + B_{k,j})$$

Viterbi算法的矩阵形式（一行公式！）：
$$\delta_t = \delta_{t-1} \otimes T + B_t$$

其中 T 是转移得分矩阵，Bₜ 是时刻t的发射得分向量。

### 9.4 双轨制：推理 vs 训练

| 任务 | 代数 | 核心操作 |
|------|------|---------|
| **推理（Viterbi解码）** | Max-Plus | $\max$（只选最优路径） |
| **训练（前向-后向）** | Log-Sum-Exp（普通） | $\sum$（考虑所有路径） |

**统一原理**：温度 → 0 时，Log-Sum-Exp → Max：
$$\lim_{\tau \to 0} \tau \log\sum_i \exp(x_i/\tau) = \max_i x_i$$

### 9.5 核心启发

1. **Viterbi本质** = Max-Plus矩阵乘法，所有序列标注解码都在做同一件事
2. **唯一区别**：转移矩阵和发射向量从哪来（HMM是概率统计，CRF是特征学习）
3. **推理与训练分离**：推理走Max-Plus（确定性最强路径），训练走Log-Sum-Exp（考虑所有路径的软分配）

---

## 附录A：公式速查表

### 前向-后向

| 算法 | 初始化 | 递推 | 终止 |
|------|--------|------|------|
| **Forward α** | α₁(i) = πᵢ·bᵢ(o₁) | αₜ(j)=[Σᵢαₜ₋₁(i)·aᵢⱼ]·bⱼ(oₜ) | P(O)=Σᵢα_T(i) |
| **Backward β** | β_T(i)=1 | βₜ(i)=Σⱼaᵢⱼ·bⱼ(oₜ₊₁)·βₜ₊₁(j) | P(O)=Σᵢπᵢ·bᵢ(o₁)·β₁(i) |

### Viterbi

| Step | 公式 |
|------|------|
| 递推 | δₜ(j) = maxᵢ[δₜ₋₁(i)·aᵢⱼ] · bⱼ(oₜ) |
| 回溯指针 | ψₜ(j) = argmaxᵢ[δₜ₋₁(i)·aᵢⱼ] |
| 回溯 | s*ₜ₋₁ = ψₜ(s*ₜ) |

### Baum-Welch (EM)

| 统计量 | 公式 |
|--------|------|
| γₜ(i) | αₜ(i)·βₜ(i) / P(O) |
| ξₜ(i,j) | αₜ(i)·aᵢⱼ·bⱼ(oₜ₊₁)·βₜ₊₁(j) / P(O) |
| π̂ᵢ | γ₁(i) |
| âᵢⱼ | Σₜξₜ(i,j) / Σₜγₜ(i) |
| b̂ⱼ(v) | Σₜγₜ(j)·𝟙(oₜ=v) / Σₜγₜ(j) |

### CRF

$$P(y|x) = \frac{1}{Z(x)}\exp\left(\sum_j \lambda_j F_j(y, x)\right)$$

### Max-Plus Viterbi

$$\delta_t = \delta_{t-1} \otimes T + B_t$$
$$\delta_t(j) = B_t(j) + \max_i[\delta_{t-1}(i) + T_{i,j}]$$

---

## 附录B：常见考试题型

### 题型1：HMM前向手算
**典型问题**：给Π/A/B和观测序列，计算P(O\|λ)

**答题模板**：画α表格（T×N），逐行填入计算过程

### 题型2：Viterbi解码手算
**典型问题**：同上，求最优状态序列

**答题模板**：画δ+ψ双重表格，每行记录δ值和来源ψ，终止后回溯

### 题型3：Baum-Welch E步
**典型问题**：已知α和β值，计算γₜ(i)和ξₜ(i,j)

**答题模板**：代入公式，写出分母P(O)和分子

### 题型4：概念辨析
**典型问题**："比较HMM和CRF，至少三点"

**答题模板**：生成vs判别、特征自由度、标签偏置、训练方法

### 题型5：三个基本问题简答
**答题模板**：评估→Forward(P(O))、解码→Viterbi(最优路径)、学习→Baum-Welch(EM)

---

> **跨文档链接**：
> - 模糊推理、贝叶斯方法 → [02-概率推理与不确定性处理](02-概率推理与不确定性处理.md)
> - RNN/LSTM（神经序列模型） → [03-神经网络：从感知机到深度学习](03-神经网络：从感知机到深度学习.md) §七
> - 马尔可夫链（HMM的简化基础） → [02-概率推理与不确定性处理](02-概率推理与不确定性处理.md) §五
> - 强化学习的MDP（带动作的马尔可夫链） → [06-强化学习](06-强化学习：从老虎机到深度RL.md) §三
