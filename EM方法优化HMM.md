我们以 **BIO 分词任务**（将句子中的每个字标注为 B（词首）、I（词内）、O（词外））为具体场景，来系统简述 HMM 的 EM 算法（即 **Baum-Welch 算法**）。

---

## 1. 目的（我们要做什么）

- **HMM 建模**：  
  观测序列 $ O = (o_1, ..., o_T) $ = 句子中的字（如“我喜欢学习”）  
  隐状态序列 $ S = (s_1, ..., s_T) $ = 对应的 BIO 标签（如“B I O B I”）

- **HMM 参数**（需要学习）：
  - $ \pi_i $：初始状态概率（第一个字是 B/I/O 的概率）
  - $ a_{ij} $：转移概率（从状态 i 到状态 j）
  - $ b_j(o) $：发射概率（状态 j 生成某个字 o 的概率）

- **EM 的目的**：  
  当我们**只有观测句子（字）**，没有人工标注的 BIO 标签时，用 EM 算法**无监督地**估计 HMM 参数，使得模型能最好地解释观测数据。

---

## 2. EM 在 HMM 中的核心思想（直观）

- **E 步（期望）**：  
  用当前参数，计算**每个时刻 t 属于每个状态的概率**（以及相邻时刻状态对的概率），即“补齐”缺失的隐状态信息。

- **M 步（最大化）**：  
  用这些“软计数”（期望计数）重新估计 $ \pi, a, b $，使得似然最大。

- 迭代直到收敛。

---

## 3. 重要符号定义

- $ \alpha_t(i) = P(o_1...o_t, s_t=i) $：前向概率
- $ \beta_t(i) = P(o_{t+1}...o_T | s_t=i) $：后向概率
- $ \gamma_t(i) = P(s_t=i | O) $：时刻 t 处于状态 i 的后验概率
- $ \xi_t(i,j) = P(s_t=i, s_{t+1}=j | O) $：时刻 t 为 i，t+1 为 j 的后验概率

---

## 4. 重点推导（E 步的统计量）

### (1) 计算 $ \gamma_t(i) $

利用贝叶斯：

$$
\gamma_t(i) = \frac{\alpha_t(i) \beta_t(i)}{P(O)}
$$

其中 $ P(O) = \sum_k \alpha_T(k) $。

### (2) 计算 $ \xi_t(i,j) $

由 HMM 的联合概率：

$$
P(s_t=i, s_{t+1}=j, O) = \alpha_t(i) \cdot a_{ij} \cdot b_j(o_{t+1}) \cdot \beta_{t+1}(j)
$$

因此：

$$
\xi_t(i,j) = \frac{\alpha_t(i) \cdot a_{ij} \cdot b_j(o_{t+1}) \cdot \beta_{t+1}(j)}{P(O)}
$$

这两个公式是 E 步的核心产出。

---

## 5. M 步推导（参数重估公式）

我们用期望计数来替代完整数据下的极大似然估计。

- **初始概率**（第一个时刻的期望计数）：
$$
\pi_i^{new} = \gamma_1(i)
$$

- **转移概率**（从 i 到 j 的期望次数 / 离开 i 的期望次数）：
$$
a_{ij}^{new} = \frac{\sum_{t=1}^{T-1} \xi_t(i,j)}{\sum_{t=1}^{T-1} \gamma_t(i)}
$$

- **发射概率**（状态 j 生成词 v 的期望次数 / 状态 j 出现的期望次数）：
$$
b_j(v)^{new} = \frac{\sum_{t=1}^{T} \gamma_t(j) \cdot \mathbf{1}(o_t = v)}{\sum_{t=1}^{T} \gamma_t(j)}
$$

> **推导目的说明**：  
> 这些公式本质上就是**在完整数据（带标注）下的 MLE 估计**，只是把“硬计数”（某个时刻确定属于哪个状态）换成了“软计数”（概率加权）。这就是 EM 的“用期望补全缺失数据”的思想。

---

## 6. BIO 分词任务中的具体例子

句子：`我 喜 欢 学 习`（观测）

假设当前参数下，E 步算出：

- $ \gamma_2(B)=0.7 $，$ \gamma_2(I)=0.3 $（第二个字“喜”有 0.7 概率是词首）
- $ \xi_2(B, I)=0.5 $（“喜”是 B 且“欢”是 I 的概率）

M 步就会：
- 把“喜”作为 B 的发射计数增加 0.7，作为 I 增加 0.3
- 把转移 B→I 的计数增加 0.5

这样反复迭代，最终学到合理的转移矩阵（如 B→I 概率大）和发射矩阵（如“习”常作为 I 或 B）。

---

## 7. 整体算法流程总结

1. 初始化 $ \pi, a, b $（随机或均匀）
2. **E 步**：用前向后向算法计算 $ \gamma, \xi $
3. **M 步**：用上面公式更新 $ \pi, a, b $
4. 计算对数似然 $ \log P(O) $，检查是否收敛
5. 若未收敛，回到 E 步

---

## 8. 为什么要重点推导这些公式（核心目的再强调）

- **推导的目的不是数学游戏**，而是要展示：  
  **HMM 的 EM 算法本质上是在做“软 Viterbi 训练”**——它不把每个时刻硬性指派给一个状态，而是保留概率分布，从而在无标注数据下仍能渐进地估计出正确的生成和转移模式。
- 对于 BIO 分词，这意味着我们可以**仅凭大量未分词的文本**，自动学习到“B 后面大概率接 I，I 后面大概率接 B 或 I，O 前后多接 O”等语言学规律。
