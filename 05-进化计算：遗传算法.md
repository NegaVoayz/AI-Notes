# 05-进化计算：遗传算法

> **本文定位**：开卷考试中"遗传算法"相关题目的核心参考。涵盖GA基本流程、轮盘赌手算、交叉/变异、模式定理。
> **老师偏好**：一轮GA手算（选择→交叉→变异）、模式定理公式解释、格雷编码优势。

---

## 目录

- [一、遗传算法概述](#一遗传算法概述)
- [二、基本流程与编码](#二基本流程与编码)
- [三、选择操作](#三选择操作)
- [四、交叉操作](#四交叉操作)
- [五、变异操作](#五变异操作)
- [六、完整一代手算示例](#六完整一代手算示例)
- [七、模式定理（Schema Theorem）](#七模式定理schema-theorem)
- [八、TSP中的GA操作](#八tsp中的ga操作)
- [九、GA与其他方法的对比](#九ga与其他方法的对比)
- [附录A：公式速查表](#附录a公式速查表)
- [附录B：常见考试题型](#附录b常见考试题型)

---

## 一、遗传算法概述

### 1.1 核心思想

> **直观理解**：遗传算法不像数学家那样求解，而是像**农夫育种**——每代保留最好的个体，让它们"交配"产生后代，偶尔有"突变"。经过很多代，种群自然进化出最优解。

**生物学类比**：

| 生物进化 | 遗传算法 |
|---------|---------|
| 个体 | 候选解 |
| 染色体 | 编码的二进制串/排列 |
| 基因 | 每一位/每一个元素 |
| 适应度 | 目标函数值 |
| 交配 | 交叉（Crossover） |
| 突变 | 变异（Mutation） |
| 世代 | 迭代轮次 |

### 1.2 适用场景

- 高维、非线性、离散优化问题
- 目标函数不可导或没有解析形式
- 搜索空间巨大且复杂

### 1.3 伪代码

```
t = 0
初始化种群 P(t)
while 终止条件未满足:
    计算每个个体的适应度
    根据适应度选择个体（Selection）
    对配对个体进行交叉，产生子代（Crossover）
    对子代进行变异（Mutation）
    用子代替换部分或全部原个体
    t = t + 1
```

---

## 二、基本流程与编码

### 2.1 编码方式

| 编码类型 | 表示 | 适用场景 |
|---------|------|---------|
| **二进制编码** | 5位二进制 → 0~31的整数 | 函数优化 |
| **排列编码** | 城市顺序 (1,5,3,2,4) | TSP |
| **格雷编码** | 相邻值只差1位 | 减少Hamming距离影响 |

### 2.2 格雷编码 vs 二进制编码

> **直观理解**：二进制中7(00111)和8(01000)差4位——从7到8需要同时翻转4个位，极难通过变异一步到达。格雷码中相邻整数始终只差1位——任何单点变异都只产生一个小步变化。

| 十进制 | 二进制 | 格雷码 |
|--------|--------|--------|
| 7 | 00111 | 00100 |
| 8 | 01000 | 01100 |
| Hamming距离 | **4** | **1** |

> **考试点**：解释为什么GA中常用格雷编码——减少"Hamming悬崖"，让相邻值更容易通过变异互相到达。

---

## 三、选择操作

### 3.1 轮盘赌选择（Roulette Wheel·必考手算）

> **直观理解**：每个个体的"中奖概率"与其适应度成正比。适应度高的占据轮盘更大的扇形，更容易被选中。

**选择概率公式**：
$$p_i = \frac{f(i)}{\sum_{j=1}^{n} f(j)}$$

### 3.2 指令级手算步骤

| Step | 操作 | 说明 |
|------|------|------|
| 1 | 计算所有个体的适应度 f(i) | 代入目标函数 |
| 2 | 计算总适应度 Σf(i) | 分母 |
| 3 | 计算每个个体的选择概率 pᵢ = f(i)/Σf(j) | 0~1之间的值 |
| 4 | 计算累积概率：cp₁=p₁, cp₂=p₁+p₂, ... | 用于轮盘指针定位 |
| 5 | 生成N个[0,1]随机数 | 每个随机数选一个个体 |
| 6 | 根据随机数落在哪个区间确定选中个体 | 模拟N次轮盘转动 |

**手算口诀**：适应度占比 → 累积边界 → 随机数指哪选哪。

---

## 四、交叉操作

### 4.1 单点交叉（必考手算）

**指令级**：在随机位置把两个父代"切开"，交换后半部分。

**例**：交叉点=3（即第3位后切开）

| 父代 | 交叉前 | 交叉后=子代 |
|------|--------|------------|
| p₁ | 110 ║ 00 | 110 ║ 11 → **11011** |
| p₂ | 100 ║ 11 | 100 ║ 00 → **10000** |

### 4.2 多点交叉

在多个位置切开，交替拼接。原理同单点，切点更多。

---

## 五、变异操作

### 5.1 二进制位翻转

给定变异位置，翻转该位：0→1，1→0。

**例**：个体 11011，变异位置=2（第2位）→ 1**0**011

### 5.2 TSP变异

| 操作 | 描述 | 例子 |
|------|------|------|
| **交换变异** | 交换两个位置 | (1,5,3,2,4) → (1,**2**,3,**5**,4) |
| **逆序变异** | 翻转一个子段 | (1,5,3,2,4) → (1,**2,3,5**,4) |

---

## 六、完整一代手算示例（必练·必考）

### 6.1 问题：最大化 f(x)=x²，x∈[1,31]，5位二进制编码

**Step 1: 初始种群**

| 编号 | 二进制 | 十进制 | 适应度 f(x)=x² |
|------|--------|--------|----------------|
| 1 | 01101 | 13 | 169 |
| 2 | 11000 | 24 | 576 |
| 3 | 01000 | 8 | 64 |
| 4 | 10011 | 19 | 361 |
| **总和** | | | **1170** |
| **平均** | | | 1170/4 = 292.5 |

**Step 2: 选择（轮盘赌）**

| 编号 | 适应度 | 概率 pᵢ | 累积概率 | 随机数(假设) | 选中 |
|------|--------|---------|---------|-------------|------|
| 1 | 169 | 0.14 | 0.14 | — | — |
| 2 | 576 | **0.49** | 0.63 | 0.33→选2, 0.81→选2 | 2次 |
| 3 | 64 | 0.06 | 0.69 | — | — |
| 4 | 361 | **0.31** | 1.00 | 0.12→选1, 0.55→选4 | 1次, 1次 |

**选择结果（4个个体）**：1, 2, 2, 4

| 新编号 | 个体 | 二进制 |
|--------|------|--------|
| 1' | 原1号 | 01101 |
| 2' | 原2号 | 11000 |
| 3' | 原2号 | 11000 |
| 4' | 原4号 | 10011 |

> **注意**：高适应度个体(2号)被选了两次，低适应度个体(3号)被淘汰——这就是"优胜劣汰"。

**Step 3: 交叉（单点，交叉概率 p_c=1 假设全交叉）**

配对：1'×2'，3'×4'

| 配对 | 父代 | 交叉点 | 子代 |
|------|------|--------|------|
| 1'×2' | 011|01 × 110|00 (点=2) | 01100, 11001 |
| 3'×4' | 11|000 × 10|011 (点=1) | 11011, 10000 |

**Step 4: 变异（位翻转，假设变异发生在子代1的第3位）**

| 子代 | 变异前 | 变异位 | 变异后 | 十进制 | f(x) |
|------|--------|--------|--------|--------|------|
| 1 | 01100 | 3→1 | 01**1**00→01**0**00 | 8 | 64 |
| 2 | 11001 | — | 11001 | 25 | 625 |
| 3 | 11011 | — | 11011 | 27 | 729 |
| 4 | 10000 | — | 10000 | 16 | 256 |

**一代结束**：新种群平均适应度 = (64+625+729+256)/4 = 418.5（比初始292.5提高了！）

---

## 七、模式定理（Schema Theorem）

### 7.1 模式（Schema）定义

> **直观理解**：模式就是加通配符(*)的"模板"。**1\*0\*\*1** 表示一个二进制串：第1位必须=1，第3位必须=0，第6位必须=1，其他位随意。

### 7.2 模式属性

| 属性 | 符号 | 定义 | 例子：模式 **1\*0\*\*1** |
|------|------|------|--------------------------|
| **阶** | O(s) | 固定位置的数量 | O = 3（位置1,3,6） |
| **定义长度** | $\delta(s)$ | 第一个和最后一个固定位之间的距离 | $\delta = 6 - 1 = 5$ |
| **长度** | ℓ | 染色体总长度 | ℓ = 6 |

### 7.3 模式定理公式（必背）

$$E[m(s, t+1)] = m(s, t) \cdot \frac{\bar{f}(s)}{\bar{f}} \cdot \left[1 - p_c \cdot \frac{\delta(s)}{\ell - 1} - O(s) \cdot p_m\right]$$

| 项 | 含义 |
|----|------|
| $m(s,t)$ | 第t代匹配模式s的个体数 |
| $\frac{\bar{f}(s)}{\bar{f}}$ | 模式相对适应度比 → **选择压力** |
| $p_c \cdot \frac{\delta(s)}{\ell - 1}$ | 交叉破坏概率 → 定义长度的个体更易被切断 |
| $O(s) \cdot p_m$ | 变异破坏概率 → 固定位多的更容易被变异破坏 |

> **公式解读**：下一代期望个体数 = 当前个体数 × **选择优势** × **存活概率**（不被交叉破坏、不被变异破坏）。

### 7.4 建筑块假设（Building Block Hypothesis）

> **短定义长度、低阶、高适应度**的模式（建筑块）在GA运行中**指数级增长**，最终组合成最优解。

| 为什么"短"好？ | 交叉点落在定义长度内的概率小 → 不易被破坏 |
| 为什么"低阶"好？ | 固定位少 → 变异破坏的概率小 |
| 为什么"高适应度"好？ | 选择概率高 → 被选中的机会大 |

### 7.5 模式定理手算例题

**例题**：种群大小=100，模式 s = \*1\*0\*（ℓ=5, O=2, $\delta$=2），当前有10个匹配。$\bar{f}(s)/\bar{f}=2$，p_c=0.6, p_m=0.01。

E[m(s,t+1)] = 10 $\times$ 2 $\times$ [1 − 0.6$\times$(2/4) − 2$\times$0.01] = 20 $\times$ [1 − 0.3 − 0.02] = 20 $\times$ 0.68 = **13.6**

> 期望数量从10增长到13.6——尽管交叉和变异破坏了一部分，但选择优势(×2)仍然推动增长。

---

## 八、TSP中的GA操作

### 8.1 PMX（部分映射交叉）

> **直观理解**：TSP的解是城市序列，普通交叉会产生重复城市（非法解）。PMX保证子代仍然是**合法的排列**。

**PMX步骤（指令级）**：
1. 选两个交叉点，标记中间段
2. 将p1的中间段复制到子代对应位置
3. 从p2中按顺序填入未出现的城市
4. 遇到冲突时用映射关系找到替代

**例题**：p₁=(1 9 2 ║ 4 6 5 7 ║ 8 3), p₂=(4 5 9 ║ 1 8 7 6 ║ 2 3)，交叉点在位置3和7

**映射关系**：4↔1, 6↔8, 5↔7, 7↔6

| Step | 操作 |
|------|------|
| 1 | 从p2开始，从左到右填非中间段的数 |
| 2 | 遇到冲突→查映射表找替代 |
| 3 | 中间段保持p1不变 |
| 4 | 得到子代 |

### 8.2 TSP变异

- **交换**：随机选两个位置交换
- **逆序（Inversion）**：随机选一段，反转顺序

---

## 九、GA与其他方法的对比

### 9.1 GA vs 随机搜索

| | GA | 随机搜索 |
|---|---|---|
| 搜索方向 | **有方向性**（利用历史信息） | 无方向性（纯随机） |
| 效率 | 更高（利用建筑块） | 低（可能无限重复） |
| 全局性 | 较好（种群+多样性机制） | 好（完全随机） |

### 9.2 GA vs 强化学习

| | GA | RL |
|---|---|---|
| 搜索方式 | 群体并行搜索 | 个体序贯交互 |
| 反馈 | 整体适应度 | 每步奖励 |
| 策略 | 无显式策略 | 有策略π(a\|s) |
| 适用 | 优化问题（参数搜索） | 序贯决策问题 |

### 9.3 GA vs 专家系统

| | GA | 专家系统 |
|---|---|---|
| 知识来源 | 从进化搜索中自动发现 | 人工编码规则 |
| 可解释性 | 低（解是数字） | 高（推理链可追踪） |
| 适用 | 可以用GA调优专家系统的规则权重 | — |

---

## 附录A：公式速查表

| 公式 | 用途 |
|------|------|
| $p_i = \frac{f(i)}{\sum f(j)}$ | 选择概率 |
| $\bar{f} = \frac{\sum f(j)}{n}$ | 平均适应度 |
| $E[m(s,t+1)] = m(s,t) \cdot \frac{\bar{f}(s)}{\bar{f}} \cdot [1 - p_c\frac{\delta(s)}{\ell-1} - O(s)p_m]$ | 模式定理 |
| O(s) = 固定位置数 | 模式阶 |
| $\delta(s)$ = 首末固定位距离 | 模式定义长度 |

---

## 附录B：常见考试题型

### 题型1：轮盘赌选择手算
**典型问题**：给4个个体的适应度表，手算选择结果

**答题模板**：适应度→概率→累积→随机数判定

### 题型2：单点交叉手算
**典型问题**：给两个父代和交叉点，求子代

### 题型3：模式定理计算
**典型问题**：给模式和参数，计算下一代期望个体数

### 题型4：概念解释
- "解释建筑块假设"
- "为什么格雷编码有利于GA？"
- "什么是模式？阶和定义长度分别是什么？"

### 题型5：对比辨析
- GA vs 随机搜索
- GA vs RL

---

> **跨文档链接**：
> - 强化学习 → [06-强化学习：从老虎机到深度RL](06-强化学习：从老虎机到深度RL.md)
> - 专家系统 → [01-符号主义AI](01-符号主义AI：逻辑、知识表示与专家系统.md) §九~§十
> - 模糊逻辑 → [02-概率推理与不确定性处理](02-概率推理与不确定性处理.md) §六
