# 02-概率推理与不确定性处理

> **本文定位**：开卷考试中"不确定性推理"相关题目的核心参考。涵盖贝叶斯决策、确定性因子、马尔可夫链、模糊集。
> **老师偏好**：CF手算、马尔可夫稳态求解、模糊合成运算、概念辨析（CF vs 概率）。

---

## 目录

- [一、不确定性概述](#一不确定性概述)
- [二、经典悖论与推理局限](#二经典悖论与推理局限)
- [三、贝叶斯决策理论](#三贝叶斯决策理论)
- [四、确定性因子（Certainty Factor）](#四确定性因子certainty-factor)
- [五、马尔可夫链](#五马尔可夫链)
- [六、模糊集与模糊逻辑](#六模糊集与模糊逻辑)
- [附录A：公式速查表](#附录a公式速查表)
- [附录B：常见考试题型与答题模板](#附录b常见考试题型与答题模板)
- [附录C：教师可能课堂扩展内容](#附录c教师可能课堂扩展内容)

---

## 一、不确定性概述

### 1.1 不确定性的来源与错误类型

| 示例 | 错误类型 | 原因 |
|------|---------|------|
| "Turn the valve off" | 歧义（Ambiguity） | 哪个阀门？ |
| "Turn valve-1" | 不完整（Incompleteness） | 哪个方向？ |
| "Turn valve-1 off" | 不正确（Incorrectness） | 正确的是"开" |
| "Valve is stuck" | 假阳性 / Type I Error | 阀没卡住但判断卡住了 |
| "Valve is not stuck" | 假阴性 / Type II Error | 阀卡住了但判断没卡住 |
| "Turn valve-1 to 5" | 不精确（Imprecision） | 正确的是5.4 |
| "Turn valve-1 to 5.4" | 不准确（Inaccuracy） | 正确的是9.2 |

> **考试辨析**：Type I Error = 假阳性（误报），Type II Error = 假阴性（漏报）。医检中：假阳性="没病说有病"，假阴性="有病说没病"。

### 1.2 演绎 vs 归纳

| | 演绎（Deduction） | 归纳（Induction） |
|---|---|---|
| **方向** | 一般→特殊 | 特殊→一般 |
| **结论** | 必然为真（前提真） | 具有置信度，非必然 |
| **例子** | 所有人会死→苏格拉底会死 | 磁盘从未崩溃→磁盘永不崩溃 |
| **问题** | — | 条件概率估计难、证据组合复杂、集体反驳 |

---

## 二、经典悖论与推理局限

### 2.1 布里丹之驴（Buridan's Ass）

> **场景**：一头驴站在两堆完全相同的干草中间，无法决定吃哪一堆 → 饿死。
> **启示**：完全对称的选择会导致理性决策瘫痪——不确定性下的决策困境。

### 2.2 彩票悖论（Lottery Paradox）

> **场景**：1000张彩票，1张中奖。对于每一张单独彩票，理性上应该相信"它不会中奖"（概率99.9%）。但理性上也应该相信"有一张会中奖"（概率100%）。
> **矛盾**：如果相信每张都不中奖（通过合取），则推出"所有都不中奖"——与"有一张中奖"矛盾。
> **启示**：高概率信念的合取不是封闭的——很多"几乎确定"的事合起来会导出矛盾。

### 2.3 前言悖论（Preface Paradox）

> **场景**：作者在书的前言中写道"本书可能有错误"，但同时确信书中每条陈述都是正确的。
> **矛盾**：如果每条陈述都正确，则"本书有错误"这条陈述本身是错误的。
> **启示**：信念集合可以是"每个都合理但合起来不一致"的。

### 2.4 反驳循环（Defeat Cycle）

> **场景**：Jones说Smith不可靠 → Smith说Peter不可靠 → Peter说Jones不可靠
> **启示**：互相指控不可靠的循环导致无法确定任何人的可信度。

### 2.5 统计检验的经典问题

- **点零假设问题**：$H_0$: $\theta$=0.5 几乎不可能恰好为真 → 大样本总会拒绝$H_0$（不论实际是否有意义）
- **拟合检验问题**：模型几乎不可能是**完全**正确的 → 大样本总会拒绝任何模型

> **考试关注点**：解释任一个悖论的含义和启示（3-5句话）。

---

## 三、贝叶斯决策理论

### 3.1 贝叶斯定理（核心·必背）

> **直观理解**：贝叶斯定理本质上是一个"信念更新公式"——新证据来了，把我的旧信念（先验）按证据的强度更新为新信念（后验）。

**标准形式**：

$$P(D_i | E) = \frac{P(E|D_i) \cdot P(D_i)}{\sum_j P(E|D_j) \cdot P(D_j)}$$

| 符号 | 名称 | 含义 |
|------|------|------|
| $P(D_i)$ | 先验概率 | 看到证据前对假设的信念 |
| $P(E \mid D_i)$ | 似然（Likelihood） | 假设$D_i$为真时证据出现的概率 |
| $P(D_i \mid E)$ | 后验概率 | 看到证据后修正的信念 |
| $\sum_j P(E \mid D_j)P(D_j)$ | 归一化常数 | 证据的总概率（所有假设路径之和） |

### 3.2 手算指令（Step by Step）

**例题**：PROSPECTOR矿物勘探

| Step | 操作 | 计算 |
|------|------|------|
| 1 | 写出先验：P(O)=0.6, P(O')=0.4 | 有矿60%，无矿40% |
| 2 | 写出似然：P(+$\mid$O)=0.8, P(−$\mid$O)=0.2; P(+$\mid$O')=0.1, P(−$\mid$O')=0.9 | 测试精度 |
| 3 | 计算联合概率：P(+$\cap$O)=0.8$\times$0.6=0.48, P(+$\cap$O')=0.1$\times$0.4=0.04 | 分子 |
| 4 | 计算P(+)：P(+)=0.48+0.04=0.52 | 分母（归一化） |
| 5 | 计算后验：P(O$\mid$+)=0.48/0.52=0.923 | 测试阳性→有矿概率92.3% |

### 3.3 增量证据更新

$$P(D_i | E_1 \cap E_2) = \frac{P(E_2 | D_i \cap E_1) \cdot P(D_i | E_1)}{\sum_j P(E_2 | D_j \cap E_1) \cdot P(D_j | E_1)}$$

> **直观理解**：第一次更新后的后验，成为第二次更新的先验。贝叶斯更新是**递进的**而非一次性的。

### 3.4 贝叶斯分析的完整示例（农民作物选择·可能手算）

**问题设定**：农民选作物 $a_1$(抗旱, 损失=200−2$\theta$) 或 $a_2$(高产, 损失=3000−10$\theta$)，$\theta$为降水量。

**决策规则 $\delta$**：若天气预报 x < 400 则选 $a_1$，否则选 $a_2$。

**手算框架（指令级）**：

| Step | 操作 |
|------|------|
| 1 | 写出损失函数 l($\theta$,a) |
| 2 | 写出条件密度 f(x$\mid\theta$)（柯西分布形式） |
| 3 | 对每个$\theta$计算决策风险 R($\theta$,$\delta$) = $\int$ l($\theta$,$\delta$(x))·f(x$\mid\theta$) dx |
| 4 | 查先验分布表 $\pi$($\theta$) |
| 5 | 计算贝叶斯风险 r($\pi$,$\delta$) = $\sum_\theta$ R($\theta$,$\delta$)·$\pi$($\theta$) |

**先验分布表（讲义数据）**：

| $\theta$ | 0 | 100 | 200 | 300 | 400 | 500 | 600 | 700 | 800 |
|---|---|---|---|---|---|---|---|---|---|
| $\pi$($\theta$) | 0 | 0.052 | 0.104 | 0.153 | 0.178 | 0.204 | 0.153 | 0.104 | 0.052 |

**风险积分**（概念层面理解）：
$$R(\theta, \delta) = \int_0^{400} f(x|\theta)(200-2\theta) dx + \int_{400}^{\infty} f(x|\theta)(3000-10\theta) dx$$

最终贝叶斯风险 r($\pi$,$\delta$) = **-1176**（负值表示期望收益为正，决策明智）。

> **考试关注点**：可能不要求完整计算风险积分（涉及arctan），但会考框架理解——写出损失函数、先验代入、加权平均求贝叶斯风险的完整步骤。

### 3.5 贝叶斯方法的三大困难

1. **条件概率爆炸**：每新增一个证据需要估计的条件概率指数增长
2. **互补概率直觉冲突**：专家愿意给 P(H|E)=0.7，但不接受 P($\lnot$H|E)=0.3（因为$\lnot$H包含了太多替代假设）
3. **多假设情况下不可接受**：当备选假设很多时，很难逐一赋予有意义的概率

---

## 四、确定性因子（Certainty Factor）

### 4.1 为什么需要CF？

> **直观理解**：医生不愿意说"这个病人有70%概率得肺炎，30%概率不是肺炎"。因为"不是肺炎"包含了感冒/肺癌/支气管炎等无数可能——大脑不习惯把互补置信度分配给一个笼统的否定。CF的设计**刻意绕过了互补概率约束**。

### 4.2 定义（必考）

$$CF(H, E) = MB(H, E) - MD(H, E)$$

- **MB**（Measure of Belief）：因证据E而增加的信任度，**范围 [0,1]**
- **MD**（Measure of Disbelief）：因证据E而增加的不信任度，**范围 [0,1]**
- **CF**：**范围 [-1, 1]**，+1=确定真，-1=确定假，0=无证据

**数学定义**：

当 P(H|E) > P(H)（证据增强信念）：
$$MB(H,E) = \frac{P(H|E) - P(H)}{1 - P(H)}, \quad MD(H,E) = 0$$

当 P(H|E) < P(H)（证据削弱信念）：
$$MD(H,E) = \frac{P(H) - P(H|E)}{P(H)}, \quad MB(H,E) = 0$$

> **分子解释**：MB的分子 P(H|E)-P(H) 是"信念增加了多少"；分母 1-P(H) 是"本来还能增加的最大空间"。所以MB是"在还能增加的范围内，实际增加了多少比例"。

### 4.3 CF手算例题（指令级·必考）

**题目**：已知 P(H)=0.3，P(H|E)=0.8，求 CF(H,E)。

| Step | 操作 | 计算 |
|------|------|------|
| 1 | 比较 P(H$\mid$E) 和 P(H) | 0.8 > 0.3 → 证据增强信念 → MD=0 |
| 2 | 计算 MB = [P(H$\mid$E)-P(H)]/[1-P(H)] | MB = (0.8-0.3)/(1-0.3) = 0.5/0.7 = 0.714 |
| 3 | CF = MB - MD | CF = 0.714 - 0 = **0.714** |

**验证**：再算反向——已知 P(H)=0.3, P(H|E)=0.1
| Step | 操作 | 计算 |
|------|------|------|
| 1 | 比较 | 0.1 < 0.3 → 证据削弱信念 → MB=0 |
| 2 | MD = [P(H)-P(H$\mid$E)]/P(H) | MD = (0.3-0.1)/0.3 = 0.2/0.3 = 0.667 |
| 3 | CF = 0 - 0.667 = **-0.667** |

### 4.4 CF证据组合规则

| 组合方式 | 公式 | 例子 |
|---------|------|------|
| **AND** | `CF($E_1 \land E_2$) = min($CF_1$, $CF_2$)` | CF(0.5, 0.6) = 0.5 |
| **OR** | `CF($E_1 \lor E_2$) = max($CF_1$, $CF_2$)` | CF(0.3, 0.8) = 0.8 |
| **NOT** | `CF($\lnot E$) = -CF(E)` | CF($\lnot 0.6$) = -0.6 |

> **直观理解**：AND取min像"木桶短板"——只要有一个证据很弱整体就弱。OR取max像"一白遮百丑"——有一个强证据就够了。

### 4.5 规则链传递（CF传播）

$$CF(H, e) = CF(E, e) \times CF(H, E)$$

> **直观理解**：最终CF = 证据可信度 $\times$ 规则强度。就像"连坐制"——如果证据只有一半可信（0.5），即使规则很确定（0.9），结论也只有0.45的可信度。

### 4.6 多规则CF组合（COMB函数·必考）

设两条规则分别给出 $CF_1$ 和 $CF_2$：

$$CF_{COMB}(CF_1, CF_2) = \begin{cases} CF_1 + CF_2(1 - CF_1), & \text{两者皆正} \\ CF_1 + CF_2, & \text{一正一负（直接叠加）} \\ CF_1 + CF_2(1 + CF_1), & \text{两者皆负} \end{cases}$$

- **交换律保证**：COMB(X, Y) = COMB(Y, X)
- 范围始终在 [-1, 1]

### 4.7 MYCIN完整计算示例（必练）

> 规则: IF 菌体染色为革兰氏阳性  ($E_1$)  
>      AND 菌体形态为球菌        ($E_2$)  
>      AND 生长构型为链状        ($E_3$)  
>      THEN 链球菌 (CF=0.7)      (H)

已知 CF($E_1$,e)=0.5, CF($E_2$,e)=0.6, CF($E_3$,e)=0.3

| Step | 操作 | 结果 |
|------|------|------|
| 1 | CF(E,e) = min($CF_1$, $CF_2$, $CF_3$) | min(0.5, 0.6, 0.3) = 0.3 |
| 2 | CF(H,e) = CF(E,e) $\times$ CF(H,E) | 0.3 $\times$ 0.7 = **0.21** |

### 4.8 CF方法的局限性（考试辨析）

1. **与条件概率不一致**：
   - P($H_1$)=0.8, P($H_1$|E)=0.9 → CF=0.5
   - P($H_2$)=0.1, P($H_2$|E)=0.85 → CF=0.75
   - CF排序（0.75 > 0.5）与增量排序（+0.05 < +0.75）相同，但CF本身不是一个概率

2. **链式传递不满足概率链规则**：
   - CF(H,e) = CF(E,e)·CF(H,E) 是一条**近似公式**
   - P(H|e) $\neq$ P(H|E)·P(E|e) 通常不成立

3. **设计哲学**：CF是**非概率的启发式方法**，它绕过了概率论的公理约束，代价就是某些情况下结果不一致

---

## 五、马尔可夫链

### 5.1 核心概念

> **直观理解**：马尔可夫链就像"失忆的游客"——他下一步去哪里只取决于他现在在哪，不记得之前去过哪里。

**三个核心性质**：
1. **有限状态**：过程只能处于有限个状态之一
2. **一步转移**：每次只转移到一个状态
3. **马尔可夫性质**：P(下一状态 | 当前状态, 全部历史) = P(下一状态 | 当前状态)

### 5.2 转移矩阵与状态向量

**转移矩阵 P**（每行和为1）：
$$\mathbf{P} = \begin{bmatrix} P_{11} & P_{12} & \cdots & P_{1n} \\ P_{21} & P_{22} & \cdots & P_{2n} \\ \vdots & \vdots & \ddots & \vdots \\ P_{n1} & P_{n2} & \cdots & P_{nn} \end{bmatrix}$$

其中 $P_{ij}$ = P(下一状态=j | 当前状态=i)，即**从i到j的转移概率**。

**状态向量更新**：
$$S_{k+1} = S_k \times \mathbf{P}$$

### 5.3 稳态求解（指令级·必考）

**题目**：求转移矩阵 $\mathbf{P} = \begin{bmatrix} 0.1 & 0.9 \\ 0.6 & 0.4 \end{bmatrix}$ 的稳态分布。

**手算四步法**：

| Step | 操作 | 计算 |
|------|------|------|
| 1 | 设稳态 S=[$P_1$,$P_2$]，写出 S$\times$P=S | [$P_1$,$P_2$]·[[0.1,0.9],[0.6,0.4]] = [$P_1$,$P_2$] |
| 2 | 展开方程组 | ① 0.1$P_1$+0.6$P_2$=$P_1$ → −0.9$P_1$+0.6$P_2$=0 |
| | | ② 0.9$P_1$+0.4$P_2$=$P_2$ → 0.9$P_1$−0.6$P_2$=0（与①等价） |
| 3 | 加入归一化条件 $\sum P_i$=1 | ③ $P_1$ + $P_2$ = 1 |
| 4 | 解方程组 | 由①：$P_2$ = 1.5$P_1$，代入③：$P_1$+1.5$P_1$=1 → $P_1$=0.4, $P_2$=0.6 |

**验证**：[0.4, 0.6] $\times$ P = [0.4$\times$0.1+0.6$\times$0.6, 0.4$\times$0.9+0.6$\times$0.4] = [0.04+0.36, 0.36+0.24] = [0.4, 0.6] [OK]

### 5.4 PageRank（扩展·教师可能补充）

> PageRank是马尔可夫链的应用。随机冲浪者模型：以概率d（阻尼因子，通常0.85）沿链接跳转，以概率1-d随机跳转到任意页面。转移矩阵加上均匀随机跳转项保证**遍历性（所有状态互通）**。

---

## 六、模糊集与模糊逻辑

### 6.1 核心思想

> **直观理解**：经典逻辑是"1或0"（热/不热），模糊逻辑是"0到1之间的任意值"（0.7热）。模糊逻辑处理的是"概念边界不清晰"的问题——比如"高个子"的边界在哪里？

**隶属函数**：$\mu_A(x): X \rightarrow [0,1]$
- $\mu_A(x) = 0$ → x 完全不属于 A
- $\mu_A(x) = 1$ → x 完全属于 A
- $\mu_A(x) = 0.5$ → x 一半属于 A（模糊边界）

### 6.2 语言变量

| 变量 | 典型模糊值 |
|------|-----------|
| 身高 | 矮、短、平均、高、巨人 |
| 数量 | 几乎没有、几个、少数、许多 |
| 人生阶段 | 婴儿、幼儿、儿童、青少年、成人 |
| 颜色 | 红、蓝、绿、黄、橙 |
| 光照 | 暗、微、正常、亮、非常亮 |

### 6.3 语言修饰算子（Hedges·必考手算）

| 修饰词 | 数学操作 | 效果 |
|--------|---------|------|
| **Very F** | $\mu_{VERY}(x) = (\mu_F(x))^2$ | 收紧——"非常高"比"高"更严格 |
| **More or Less F** | $\mu_{MOL}(x) = (\mu_F(x))^{0.5}$ | 放松——"差不多高"比"高"更宽松 |
| **Plus F** | $\mu_{PLUS}(x) = (\mu_F(x))^{1.25}$ | 轻微加强 |
| **Not F** | $\mu_{NOT}(x) = 1 - \mu_F(x)$ | 取反 |

### 6.4 手算例题（指令级）

**TALL** = {0.125/5.5, 0.5/6, 0.875/6.5, 1/7}

| Step | 操作 | VERY TALL | NOT TALL |
|------|------|-----------|----------|
| 1 | 取每个元素的隶属度 | — | — |
| 2 | 应用算子 | 平方 | 1-原值 |
| 5.5ft | | 0.125² = 0.0156 | 1-0.125 = 0.875 |
| 6ft | | 0.5² = 0.25 | 1-0.5 = 0.5 |
| 6.5ft | | 0.875² = 0.7656 | 1-0.875 = 0.125 |
| 7ft | | 1² = 1 | 1-1 = 0 |

结果：
- **VERY TALL** = {0.0156/5.5, 0.25/6, 0.7656/6.5, 1/7}
- **NOT TALL** = {0.875/5.5, 0.5/6, 0.125/6.5, 0/7}

### 6.5 模糊关系与Max-Min合成（指令级·必考）

**问题**：给定 HEAVY = {0.6/140, 0.8/150, 1/160} 和 APPROXIMATELY EQUAL 关系矩阵 R，求 MORE OR LESS HEAVY。

**Step 1**：先对 HEAVY 应用 More or Less 算子（开方）：
- MORE OR LESS HEAVY = {√0.6/140, √0.8/150, √1/160} = {0.775/140, 0.894/150, 1/160}

**Step 2**：Max-Min合成公式：

$$\mu_{MOL\_HEAVY}(y) = \max_x \min(\mu_{HEAVY}(x), R(x, y))$$

**Step 3**（指令级手算）：

对于每个输出y值：
1. 对每个输入x：计算 min($\mu$(x), R(x,y))
2. 对所有x的结果取 max

| 输出y | min(0.6,R(140,y)) | min(0.8,R(150,y)) | min(1,R(160,y)) | **max** |
|-------|-------------------|-------------------|-----------------|---------|
| 120 | min(0.6,0.4)=0.4 | min(0.8,0.2)=0.2 | min(1,0.1)=0.1 | **0.4** |
| 130 | min(0.6,0.6)=0.6 | min(0.8,0.4)=0.4 | min(1,0.2)=0.2 | **0.6** |
| 140 | min(0.6,0.8)=0.6 | min(0.8,0.6)=0.6 | min(1,0.4)=0.4 | **0.6** |
| 150 | min(0.6,0.6)=0.6 | min(0.8,0.8)=0.8 | min(1,0.6)=0.6 | **0.8** |
| 160 | min(0.6,0.4)=0.4 | min(0.8,0.6)=0.6 | min(1,0.8)=0.8 | **0.8** |

> **考试关注点**：给一个小矩阵和一个隶属度向量，手算 Max-Min 合成结果。

### 6.6 模糊推理（多规则合成）

给定 n 条规则 IF $E_i$ THEN $H_i$，合成结论：

$$\mu_H = \max(\min(\mu_{E_1}), \min(\mu_{E_2}), \ldots, \min(\mu_{E_n}))$$

**复合条件分解**：
- `E_A AND E_B` → min($\mu_A$, $\mu_B$)
- `E_A OR E_B` → max($\mu_A$, $\mu_B$)
- `NOT E_A` → 1 − $\mu_A$

**飞机识别示例**：
- 雷达图像 → 计算各类型（导弹/战斗机/客机）的模糊隶属度
- 多条规则触发 → 对每条规则取min（条件隶属度）
- 最终分类 → 对各类型取max（所有规则对其支持的max）

---

## 附录A：公式速查表

### 贝叶斯

| 公式 | 用途 |
|------|------|
| $P(D_i\|E) = \frac{P(E\|D_i)P(D_i)}{\sum_j P(E\|D_j)P(D_j)}$ | 贝叶斯定理 |
| 后验 ∝ 似然 $\times$ 先验 | 比例形式记忆 |

### 确定性因子

| 公式 | 用途 |
|------|------|
| CF = MB - MD | CF定义 |
| $MB = \frac{P(H\|E)-P(H)}{1-P(H)}$（当P(H\|E)>P(H)） | MB计算 |
| $MD = \frac{P(H)-P(H\|E)}{P(H)}$（当P(H\|E)<P(H)） | MD计算 |
| CF($E_1 \land E_2$) = min($CF_1$,$CF_2$) | AND组合 |
| CF($E_1 \lor E_2$) = max($CF_1$,$CF_2$) | OR组合 |
| CF($\lnot E$) = -CF(E) | NOT |
| CF(H,e) = CF(E,e) $\times$ CF(H,E) | 规则链传递 |
| COMB($CF_1$,$CF_2$) 分段函数 | 多规则组合 |

### 马尔可夫链

| 公式 | 用途 |
|------|------|
| $S_{k+1}$ = $S_k$ $\times$ P | 状态向量更新 |
| S $\times$ P = S, $\sum P_i$ = 1 | 稳态求解 |

### 模糊运算

| 公式 | 用途 |
|------|------|
| $\mu_{VERY}$ = ($\mu_F$)$^2$ | 非常 |
| $\mu_{MOL}$ = ($\mu_F$)$^{0.5}$ | 或多或少 |
| $\mu_{NOT}$ = 1 − $\mu_F$ | 非 |
| $\mu_{out}(y) = \max_x \min(\mu_{in}(x), R(x,y))$ | Max-Min合成 |
| $\mu_H$ = max(min($\mu_{E_1}$), min($\mu_{E_2}$), ...) | 多规则模糊推理 |

---

## 附录B：常见考试题型与答题模板

### 题型1：CF手算
**典型问题**：已知 P(H)=0.4, P(H|E)=0.72, 求 CF(H,E)

**答题模板**：
1. 比较大小确定 MB还是MD
2. 代入对应公式
3. CF = MB−MD

### 题型2：MYCIN规则链计算
**典型问题**：给多个证据CF和规则CF，求最终CF(H,e)

**答题模板**：
1. AND条件 → min
2. OR条件 → max
3. 规则链传递 → 乘积
4. 多规则组合 → COMB函数

### 题型3：马尔可夫稳态求解
**典型问题**：给一个 2$\times$2 或 3$\times$3 转移矩阵，求稳态分布

**答题模板**：
1. 设 S = [$P_1$, $P_2$, ...]
2. S$\times$P=S → 写方程组
3. 加上 $\sum P_i$=1
4. 解方程

### 题型4：模糊合成运算
**典型问题**：给隶属度向量和关系矩阵，计算模糊合成

**答题模板**：
1. 对每个输出元素画一列
2. 每行计算 min(输入隶属度, 关系值)
3. 对该列所有min值取max
4. 写出输出隶属度向量

### 题型5：概念辨析题
**典型问题**："比较贝叶斯方法和确定性因子方法的优劣"

**答题模板**：
- 贝叶斯：数学严谨、公理基础；但需要大量概率、专家补概率困难
- CF：易获取、计算简单；但不完全满足概率公理、可能不一致
- 适用场景：CF适合证据少/专家直接给置信度的场景；贝叶斯适合可大量统计的场景

---

## 附录C：教师可能课堂扩展内容

### C.1 Dempster-Shafer证据理论
- CF方法的推广：不要求 P(H)+P($\lnot$H)=1
- 引入**mass函数**（基本概率分配），可给"不确定"本身分配概率
- Dempster组合规则合并多源证据

### C.2 贝叶斯网络（Bayesian Network）
- **结构**：有向无环图(DAG) + 条件概率表(CPT)
- **推理类型**：因果推理（从上到下）、诊断推理（从下到上）
- **d-分离**：判断条件独立性的图论标准
- **变量消元**：精确推理中的基本操作

### C.3 多臂老虎机与不确定性
- MAB问题中"选择哪个臂"本质上是在不确定环境下做决策
- UCB方法的"不确定性奖励项" √(2ln t / $n_i$) 明确地量化了不确定性
- Thompson Sampling用后验分布捕获认知不确定性

### C.4 模糊控制（Fuzzy Control）
- 模糊推理的工程应用：空调温控、地铁自动运行
- 模糊化 → 规则匹配 → 模糊推理 → 去模糊化
- 日本仙台地铁模糊控制系统是经典成功案例

---

> **跨文档链接**：
> - 产生式系统与专家系统 → [01-符号主义AI](01-符号主义AI：逻辑、知识表示与专家系统.md) §七~§十
> - HMM（隐马尔可夫模型）→ [04-序列建模：HMM与CRF](04-序列建模：HMM与CRF.md)
> - 多臂老虎机详细内容 → [06-强化学习](06-强化学习：从老虎机到深度RL.md) §二
> - 贝叶斯视角的曲线拟合 → [03-神经网络](03-神经网络：从感知机到深度学习.md) §五
