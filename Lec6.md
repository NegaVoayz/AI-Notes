# Lecture 6

---

## 一、遗传算法（Genetic Algorithm, GA）概述

### 1. 核心思想
- 模拟自然选择和遗传进化过程（达尔文进化论）。
- 通过“选择、交叉、变异”等操作，在解空间中搜索最优解。
- 适用于高维、非线性、离散等复杂优化问题。

### 2. 基本流程（伪代码）

```
t = 0
初始化种群 P(t)
while 终止条件未满足:
    计算种群中每个个体的适应度
    根据适应度选择个体
    对配对个体进行交叉（crossover）产生子代
    对子代进行变异（mutation）
    用子代替换原种群中部分或全部个体
    t = t + 1
```

---

## 二、关键概念

### 1. 个体表示（编码）
- 二进制编码（如 5 位二进制表示整数 0~31）。
- 也可用排列编码（如 TSP 中的城市顺序）。

### 2. 适应度函数（Fitness Function）
- 评估个体解的质量，如 $ f(x) = x^2 $。

### 3. 选择（Selection）
- **轮盘赌选择（Roulette Wheel Selection）**：按适应度比例分配选择概率。
- 适应度越高，被选中的概率越大。

### 4. 交叉（Crossover）
- 单点/多点交叉，或部分映射交叉（PMX，用于 TSP）。
- 交叉概率 $ p_c $ 控制交叉频率。

### 5. 变异（Mutation）
- 二进制变异：位翻转。
- TSP 变异：交换两个位置或片段逆序（inversion）。

---

## 三、手算示例（重点掌握）

### 1. 最大化 $ f(x) = x^2 $，$ x \in [1, 31] $，二进制编码（5 位）

**初始种群**：01101 (13), 11000 (24), 01000 (8), 10011 (19)

**适应度**：169, 576, 64, 361（总和 = 1170）

**选择概率**：0.14, 0.49, 0.06, 0.31

**轮盘赌结果**：选择后可能得到 01101, 11000, 11000, 10011（重复高适应度个体）

**交叉与变异**：讲义未详细展开，但考试可能要求手算一步交叉或变异。

---

## 四、数学公式与证明（考试重点）

### 1. 选择概率公式

$$
p_i = \frac{f(i)}{\sum_{j=1}^{n} f(j)}
$$

### 2. 种群平均适应度

$$
\bar{f} = \frac{\sum_{j=1}^{n} f(j)}{n}
$$

---

## 五、模式定理（Schema Theorem）—— 核心理论

### 1. 模式（Schema）定义
- 用 {0,1,*} 表示，* 为通配符。
- 例：$ (1 * * 0 * 1) $ 表示长度为 6 的字符串，第 1、6 位为 1，第 4 位为 0。

### 2. 模式属性
- **阶（Order）$ O(s) $**：固定位置数。
- **定义长度（Defining Length）$ \delta(s) $**：第一个和最后一个固定位置之间的距离。

### 3. 模式定理公式（重要）

$$
E[m(s, t+1)] = m(s, t) \cdot \frac{\bar{f}(s)}{\bar{f}} \cdot \left[1 - p_c \cdot \frac{\delta(s)}{l-1} - O(s) \cdot p_m \right]
$$

其中：
- $ m(s,t) $：第 t 代匹配模式 s 的个体数
- $ \bar{f}(s) $：匹配模式 s 的个体平均适应度
- $ \bar{f} $：全体个体平均适应度
- $ p_c $：交叉概率
- $ p_m $：变异概率
- $ l $：染色体长度

### 4. 模式定理含义
- **短定义长度、低阶、高适应度**的模式（称为“建筑块”）在下一代中指数增长。
- 这是 GA 有效性的理论基础。

---

## 六、TSP（旅行商问题）中的 GA 操作

### 1. 交叉示例（PMX）
- 父代：  
  $ p_1 = (1 9 2 | 4 6 5 7 | 8 3) $  
  $ p_2 = (4 5 9 | 1 8 7 6 | 2 3) $
- 从第二个交叉点开始，按 p2 顺序复制未出现的城市，得到子代。

### 2. 变异示例
- 交换两个位置，或逆序片段（inversion）。

---

## 七、扩展与延伸（可能考到的古典/相关概念）

### 1. 格雷编码（Gray Coding）
- 相邻整数二进制只差一位，减少 Hamming 距离，适合 GA。

### 2. 随机搜索 vs GA
- GA 有方向性，利用历史信息，优于纯随机搜索。

### 3. 进化硬件（Evolutionary Hardware）
- 如可编程逻辑阵列（PLA）的自动设计。

### 4. 与专家系统、强化学习（RL）的关系
- GA 可用于优化专家系统规则权重。
- 多臂老虎机问题中，GA 可用于策略搜索。
- 老师可能要求对比 GA 与 RL 的异同，或说明 GA 在早期 AI 中的应用。

---

## 八、考试常见题型（不做题型预测，但提示重点）

- **手算**：轮盘赌选择、交叉、变异（如 TSP 或二进制）。
- **证明/推导**：模式定理的推导过程。
- **解释**：模式、阶、定义长度、建筑块假设。
- **比较**：GA vs 随机搜索、GA vs RL、GA vs 专家系统。
- **应用题**：如用 GA 解决背包问题、TSP、函数优化等。

---

## 九、总结性重点清单（可直接用于开卷）

| 概念       | 说明                                             |
| ---------- | ------------------------------------------------ |
| 个体编码   | 二进制 / 排列                                    |
| 适应度函数 | 评价解的好坏                                     |
| 选择       | 轮盘赌、锦标赛等                                 |
| 交叉       | 单点、多点、PMX                                  |
| 变异       | 位翻转、交换、逆序                               |
| 模式       | 含通配符的模板                                   |
| 模式阶     | 固定位数                                         |
| 定义长度   | 首尾固定位距离                                   |
| 模式定理   | 高适应度短模式指数增长                           |
| 建筑块假设 | GA 通过组合短模式找到最优解                      |
| GA 流程    | 初始化 → 评估 → 选择 → 交叉 → 变异 → 替换 → 循环 |

---

如果需要我进一步整理成**手算示例步骤**、**模式定理推导详细过程**，或**与 RL/专家系统的对比表格**，可以继续告诉我。