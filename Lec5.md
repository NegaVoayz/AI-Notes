# Lecture 5

---

## 一、不确定性概述

### 核心概念
- **不确定性**：缺乏充分信息以做出决策的状态。
- **问题**：可能导致次优决策甚至错误决策。
- **现实意义**：所有生物都擅长处理不确定性，否则无法生存。

### 常见错误类型（讲义表格）
| 示例                | 错误类型                        | 原因               |
| ------------------- | ------------------------------- | ------------------ |
| Turn the valve off  | 歧义                            | 哪个阀门？         |
| Turn valve-1        | 不完整                          | 哪个方向？         |
| Turn valve-1 off    | 不正确                          | 正确是开           |
| Valve is stuck      | 假阳性（Type I）                | 阀未卡住但判断卡住 |
| Valve is not stuck  | 假阴性（Type II）               | 阀卡住但判断未卡住 |
| Turn valve-1 to 5   | 不精确                          | 正确是5.4          |
| Turn valve-1 to 5.4 | 不准确                          | 正确是9.2          |
| ...                 | 不可靠、随机误差、无效归纳/演绎 | ...                |

---

## 二、推理类型

### 演绎（Deduction）
- 例：所有人会死 → 苏格拉底是人 → 苏格拉底会死
- **结论必然为真**（前提为真时）

### 归纳（Induction）
- 例：我的磁盘从未崩溃 → 我的磁盘永远不会崩溃
- **结论具有置信度**，非必然

### 归纳论证中的问题
- 条件概率难以精确估计
- 证据组合复杂
- 可能会出现“集体反驳”（collective defeat）

---

## 三、经典悖论与问题

### 布里丹之驴（Buridan’s ass）
- 抉择困境：在完全相同的两个选择之间无法决策

### 彩票悖论（Lottery Paradox）
- 买彩票：单张中奖概率很低，但总有一张会中奖 → 理性上该相信“没有一张中奖”还是“至少有一张中奖”？

### 前言悖论（Preface Paradox）
- 作者对书中每一条陈述都很确信，但知道任何长书几乎必然有错误 → 是否应该否定所有断言？

### 反驳循环（Defeat Cycle）
- 讲义示例：Jones说Smith不可靠，Smith说Peter不可靠，Peter说Jones不可靠
- 循环导致无法确定谁可靠

---

## 四、经典概率与假设检验

### 古典概率
- **实验概率**：基于频率
- **主观概率**：基于信念
- **条件概率**：P(A|B) = P(A∩B)/P(B)
- **复合概率**：P(A∩B) = P(A)P(B|A)

### 假设检验问题
- 零假设 H₀: θ = 0.5
- 显著性水平：2⁻¹⁰ 在三个实验中都拒绝 H₀
- **点零假设问题**：几乎不可能恰好为真，大样本总会拒绝
- **拟合检验问题**：模型几乎不可能是完全正确的，大样本总会拒绝模型

---

## 五、贝叶斯决策

### 贝叶斯定理（重点）
$$
P(D_i | E) = \frac{P(E|D_i)P(D_i)}{P(E)} = \frac{P(E|D_i)P(D_i)}{\sum_j P(E|D_j)P(D_j)}
$$

- D_i：第 i 种疾病（假设）
- E：证据
- P(D_i)：先验概率
- P(E|D_i)：似然（条件概率）
- P(D_i|E)：后验概率

### 增量证据更新
$$
P(D_i | E_1 \cap E_2) = \frac{P(E_2 | D_i \cap E_1) P(D_i | E_1)}{\sum_j P(E_2 | D_j \cap E_1) P(D_j | E_1)}
$$

### 贝叶斯决策框架
- **先验**：主观意见
- **似然**：测试结果的条件概率
- **联合概率**：P(E∩Hᵢ) = P(E|Hᵢ)P(Hᵢ)
- **后验**：通过贝叶斯公式计算

### 应用示例（PROSPECTOR专家系统）
- 用于矿物勘探选址
- P(O)=0.6, P(O')=0.4
- P(+|O)=0.8, P(-|O)=0.2
- P(+|O')=0.1, P(-|O')=0.9

---

## 六、贝叶斯分析的数学计算（重点，可能考手算）

### 问题设定
- 农民选择作物：a₁（抗旱），a₂（高产）
- θ：下一年的降水量

### 损失函数
$$
l(\theta, a) =
\begin{cases}
200 - 2\theta, & a = a_1 \\
3000 - 10\theta, & a = a_2
\end{cases}
$$

### 条件密度（天气预报精度）
$$
f(x|\theta) =
\begin{cases}
\frac{1}{\pi} \frac{400}{400^2 + (x-\theta)^2}, & x \ge 0 \\
0, & x < 0
\end{cases}
$$
（柯西分布形式，注意归一化常数 1/π）

### 决策风险
决策规则 δ：若预报 x < 400 则选 a₁，否则选 a₂

$$
R(\theta, \delta) = \int_0^{400} f(x|\theta)(200-2\theta) dx + \int_{400}^{\infty} f(x|\theta)(3000-10\theta) dx
$$
$$
= \frac{200-2\theta}{\pi}[\arctan(\frac{400-\theta}{400}) - \arctan(\frac{-\theta}{400})] + \frac{3000-10\theta}{\pi}[\frac{\pi}{2} - \arctan(\frac{400-\theta}{400})]
$$

### 先验分布（讲义表格）
| θ    | 0    | 100   | 200   | 300   | 400   | 500   | 600   | 700   | 800   |
| ---- | ---- | ----- | ----- | ----- | ----- | ----- | ----- | ----- | ----- |
| π(θ) | 0    | 0.052 | 0.104 | 0.153 | 0.178 | 0.204 | 0.153 | 0.104 | 0.052 |

### 贝叶斯风险
$$
r(\pi, \delta) = \sum_\theta R(\theta, \delta)\pi(\theta) = -1176
$$
（负损失表示收益，农民决策是明智的）

---

## 七、贝叶斯方法的困难

### 困难1：证据积累所需概率过多
- 每次新增证据需估计更多条件概率

### 困难2：专家对互补概率的不安
- MYCIN 规则：P(H|E₁∩E₂∩E₃)=0.7
- 专家不愿接受 P(H'|...)=0.3
- **原因**：H' 包含了大量未知替代假设

### 困难3：直觉上不接受互补概率
- P(毕业 | 这课得A)=0.70
- 但不觉得 P(不毕业 | 得A)=0.30 合理
- 因为存在多种其他失败原因

---

## 八、确定性因子（Certainty Factor, CF）——MYCIN系统

### 定义
- **CF(H,E)**：证据 E 对假设 H 的确定性因子
- **MB(H,E)**：因 E 而增加的对 H 的信任度
- **MD(H,E)**：因 E 而增加的对 H 的不信任度

$$
CF(H,E) = MB(H,E) - MD(H,E)
$$

### 数学定义（重点）
$$
MB(H,E) =
\begin{cases}
1, & P(H)=1 \\
\frac{\max(P(H|E), P(H)) - P(H)}{1 - P(H)}, & \text{otherwise}
\end{cases}
$$

$$
MD(H,E) =
\begin{cases}
1, & P(H)=0 \\
\frac{\min(P(H|E), P(H)) - P(H)}{-P(H)}, & \text{otherwise}
\end{cases}
$$

等价地：
- 当 P(H|E) > P(H)：MB = [P(H|E)-P(H)]/[1-P(H)]，MD=0
- 当 P(H|E) < P(H)：MD = [P(H)-P(H|E)]/P(H)，MB=0
- 当 P(H|E) = P(H)：MB=MD=0

### 特性
| 情况        | MB        | MD    | CF     |
| ----------- | --------- | ----- | ------ |
| 确定真（P(H | E)=1）    | 1     | 0      |
| 确定假（P(H | E)=0）    | 0     | 1      |
| 无证据（P(H | E)=P(H)） | 0     | 0      |
| 范围        | [0,1]     | [0,1] | [-1,1] |

### 证据组合
- E₁ AND E₂：min[CF(E₁), CF(E₂)]
- E₁ OR E₂：max[CF(E₁), CF(E₂)]
- NOT E：-CF(E)

### 规则链传递
$$
CF(H,e) = CF(E,e) \times CF(H,E)
$$

### 多条规则支持同一假设的组合
设 CF₁, CF₂ 为两条规则对同一假设的 CF：
$$
CF_{\text{COMB}}(CF_1, CF_2) =
\begin{cases}
CF_1 + CF_2(1-CF_1), & CF_1, CF_2 > 0 \\
CF_1 + CF_2, & \text{一正一负} \\
CF_1 + CF_2(1+CF_1), & CF_1, CF_2 < 0
\end{cases}
$$
（讲义中给出的公式有符号问题，标准形式如上）
- 注意交换律：COMB(X,Y)=COMB(Y,X)

### MYCIN 规则示例
```
IF 菌体染色为革兰氏阳性
AND 菌体形态为球菌
AND 生长构型为链状
THEN 有暗示证据(0.7)该菌体是链球菌
```
- CF(H, E₁∩E₂∩E₃) = 0.7
- 若 CF(E₁,e)=0.5, CF(E₂,e)=0.6, CF(E₃,e)=0.3
- CF(E,e) = min(0.5,0.6,0.3) = 0.3
- CF(H,e) = 0.3 × 0.7 = 0.21

### CF 的困难
1. **与条件概率不一致**：P(H₁)=0.8, P(H₁|E)=0.9 → CF=0.5；P(H₂)=0.1, P(H₂|E)=0.85 → CF=0.75。CF 排序与概率排序可能矛盾。
2. **链式传递不满足概率链规则**：P(H|e) ≠ P(H|i)P(i|e) 一般情况下不成立。

---

## 九、时间推理

### 马尔可夫链（重点，可能手算）
- **有限状态**：过程只能处于有限个状态之一
- **一步转移**：每次只能转移到一个状态
- **马尔可夫性质**：下一状态只依赖于当前状态

### 转移矩阵
$$
\mathbf{P} = \begin{bmatrix}
P_{11} & P_{12} & \cdots & P_{1n} \\
P_{21} & P_{22} & \cdots & P_{2n} \\
\vdots & \vdots & \ddots & \vdots \\
P_{n1} & P_{n2} & \cdots & P_{nn}
\end{bmatrix}
$$
其中 P_{mn} = P(下一状态 = n | 当前状态 = m)

### 状态向量
$$
S_k = [P_1^{(k)}, P_2^{(k)}, \dots, P_n^{(k)}]
$$
- S_{k+1} = S_k × P（行向量右乘转移矩阵）

### 稳态（重点计算）
给定转移矩阵 $\begin{bmatrix}0.1 & 0.9 \\ 0.6 & 0.4\end{bmatrix}$

设稳态 S = [P₁, P₂]：
$$
[P_1, P_2]\begin{bmatrix}0.1 & 0.9 \\ 0.6 & 0.4\end{bmatrix} = [P_1, P_2]
$$
得：
- 0.1P₁ + 0.6P₂ = P₁ → -0.9P₁ + 0.6P₂ = 0
- 0.9P₁ + 0.4P₂ = P₂ → 0.9P₁ - 0.6P₂ = 0（与上式等价）
- P₁ + P₂ = 1
- 解得 P₁ = 0.4, P₂ = 0.6

### PageRank（扩展）
- PageRank 是马尔可夫链在 Web 排名中的应用
- $\pi^{(k)} = \pi^{(k-1)}G$，迭代至收敛

---

## 十、模糊集（Fuzzy Set）

### 核心概念
- **模糊集**：处理自然语言中的模糊概念（如“高”、“热”、“危险”）
- **隶属函数**：$\mu_A(x): X \rightarrow [0,1]$
- 区别于经典二值逻辑（0/1）

### 语言变量与典型值（讲义表格）
| 语言变量 | 典型值                         |
| -------- | ------------------------------ |
| 身高     | 矮、短、平均、高、巨人         |
| 数量     | 几乎没有、几个、少数、许多     |
| 人生阶段 | 婴儿、幼儿、儿童、青少年、成人 |
| 颜色     | 红、蓝、绿、黄、橙             |
| 光照     | 暗、微、正常、亮、非常亮       |

### 语言修饰算子（重点，可能手算）
| 修饰词         | 操作           |
| -------------- | -------------- |
| Very F         | CON(F) = F²    |
| More or Less F | DIL(F) = F^0.5 |
| Plus F         | F^1.25         |
| Not F          | 1 - F          |
| Not Very F     | 1 - CON(F)     |

### 示例计算
**TALL** = {0.125/5.5, 0.5/6, 0.875/6.5, 1/7}
- **VERY TALL** = {0.0156/5.5, 0.25/6, 0.7656/6.5, 1/7}
- **NOT TALL** = {0.875/5.5, 0.5/6, 0.125/6.5, 0/7}

### 模糊关系与合成（重点计算）
- **模糊关系**：R(x,y) 表示 x 与 y 之间的模糊关系程度
- **合成运算**：max-min 合成

例：HEAVY = {0.6/140, 0.8/150, 1/160}
APPROXIMATELY EQUAL 关系矩阵 R

MORE OR LESS HEAVY = HEAVY ∘ R
$$
\mu_{\text{MORE OR LESS HEAVY}}(y) = \max_x \min(\mu_{\text{HEAVY}}(x), R(x,y))
$$
计算得 {0.4/120, 0.6/130, 0.8/140, 0.8/150, 1/160}

### 模糊推理（重点）
给定 n 条规则：
- IF E₁ THEN H₁
- IF E₂ THEN H₂
- ...
- IF Eₙ THEN Hₙ

合成结论：
$$
\mu_H = \max(\mu_{H_1}, \mu_{H_2}, \dots, \mu_{H_n}) = \max[\min(\mu_{E_1}), \min(\mu_{E_2}), \dots, \min(\mu_{E_n})]
$$

其中 E_i 可以是复合表达式：
- E₁ = E_A AND (E_B OR NOT E_C)
- $\mu_{E_1} = \min(\mu_{E_A}, \max(\mu_{E_B}, 1 - \mu_{E_C}))$

### 飞机识别示例
- 图像 → 模糊集合（导弹/战斗机/客机的隶属度）
- 多条规则 → 取各元素最大隶属度合成

---

## 十一、可能延伸的课堂内容（推测）

### 1. 多臂老虎机（Multi-Armed Bandit）
- **探索-利用困境**：选择已知最优 arm 还是探索未知 arm
- **ε-贪婪**、**UCB**、**Thompson Sampling**
- 与贝叶斯决策的联系：后验采样

### 2. 隐马尔可夫模型（HMM，可能手算）
- **状态序列**（隐藏）+ **观测序列**（可见）
- **三要素**：初始状态分布 π、状态转移矩阵 A、观测概率矩阵 B
- **三个基本问题**：
  1. 评估：给定模型 λ 和观测序列 O，求 P(O|λ)（前向-后向算法）
  2. 解码：给定 O 和 λ，求最优状态序列（Viterbi）
  3. 学习：给定 O，估计 λ 参数（Baum-Welch）

### 3. 贝叶斯网络（Bayesian Network）
- DAG + 条件概率表（CPT）
- 因果推理、诊断推理
- d-分离、变量消元

### 4. 专家系统
- 知识库 + 推理引擎
- MYCIN、PROSPECTOR 是经典案例
- **规则基系统**、**前向/后向链推理**

---

## 十二、重点公式汇总（记忆清单）

| 公式                         | 用途                     |
| ---------------------------- | ------------------------ |
| P(Dᵢ                         | E) = P(E                 |
| 增量贝叶斯更新               | 证据积累                 |
| l(θ,a) 损失函数              | 贝叶斯决策               |
| R(θ,δ) = ∫ l(θ,δ(x))f(x      | θ)dx                     |
| r(π,δ) = Σ R(θ,δ)π(θ)        | 贝叶斯风险               |
| CF = MB - MD                 | 确定性因子               |
| MB = (P(H                    | E)-P(H))/(1-P(H)) 当 P(H |
| MD = (P(H)-P(H               | E))/P(H) 当 P(H          |
| CF(E₁ AND E₂)=min(CF₁,CF₂)   | 证据 AND                 |
| CF(E₁ OR E₂)=max(CF₁,CF₂)    | 证据 OR                  |
| CF(¬E) = -CF(E)              | 证据 NOT                 |
| CF(H,e) = CF(E,e)·CF(H,E)    | 规则链传递               |
| CF_COMB(CF₁,CF₂) 分段函数    | 多条规则组合             |
| S_{k+1} = S_k × P            | 马尔可夫链状态更新       |
| S = S × P, ΣPᵢ=1             | 稳态求解                 |
| μ_VERY_F = (μ_F)²            | 非常                     |
| μ_MORE_OR_LESS_F = (μ_F)^0.5 | 或多或少                 |
| μ_NOT_F = 1 - μ_F            | 非                       |
| μ_H = max min(μ_E)           | 模糊推理合成             |