## 1. 前向概率 $ \alpha_t(i) $ —— 从前往后看

### 定义
$$
\alpha_t(i) = P(o_1, o_2, ..., o_t, \; s_t = i \;|\; \lambda)
$$
表示：**到时刻 t 为止，观测到 $ o_1...o_t $，且时刻 t 的隐状态为 i** 的联合概率。  
（$ \lambda $ 代表全部 HMM 参数 $ \pi, a, b $）

---

### 初始化（t = 1）
$$
\alpha_1(i) = \pi_i \cdot b_i(o_1)
$$

- $ \pi_i $：状态 i 作为第一个状态的概率  
- $ b_i(o_1) $：状态 i 生成第一个字 $ o_1 $ 的概率  
> **BIO 例子**：如果“我”是观测，则 $ \alpha_1(B) = P(s_1=B) \cdot P(\text{“我”}|B) $

---

### 递推（t = 2 到 T）
$$
\alpha_{t}(j) = \left[ \sum_{i=1}^{N} \alpha_{t-1}(i) \cdot a_{ij} \right] \cdot b_j(o_t)
$$
- 先对所有前一状态 i 求和：到达状态 j 的**所有路径概率之和**（包含转移概率 $ a_{ij} $）
- 再乘以当前状态 j 发射 $ o_t $ 的概率

> **BIO 例子**：计算 $ \alpha_2(I) $ 时，要累加从 B→I 和 I→I 和 O→I 的路径，再乘以 P(“喜”|I)

---

### 终止（观测序列总概率）
$$
P(O|\lambda) = \sum_{i=1}^{N} \alpha_T(i)
$$
所有状态在最后一个时刻的概率之和，就是整个观测序列的似然。

---

## 2. 后向概率 $ \beta_t(i) $ —— 从后往前看

### 定义
$$
\beta_t(i) = P(o_{t+1}, o_{t+2}, ..., o_T \;|\; s_t = i, \lambda)
$$
表示：**给定时刻 t 状态为 i 的条件下，从 t+1 到 T 的观测序列**的条件概率。

---

### 初始化（t = T）
$$
\beta_T(i) = 1 \quad \text{（对所有 i）}
$$
因为 T 时刻之后没有观测，概率为 1（空序列）。

---

### 递推（t = T-1 到 1）
$$
\beta_t(i) = \sum_{j=1}^{N} a_{ij} \cdot b_j(o_{t+1}) \cdot \beta_{t+1}(j)
$$
- 从当前状态 i 转移到下一状态 j（概率 $ a_{ij} $）
- j 发射下一个观测 $ o_{t+1} $（概率 $ b_j(o_{t+1}) $）
- 然后继续考虑 t+1 之后的观测（$ \beta_{t+1}(j) $）

> **BIO 例子**：$ \beta_2(B) $ 表示在时刻2状态为B时，看到后续“欢学习”的概率，它等于 B→I 再发射“欢”再乘以 $ \beta_3(I) $，加上 B→B 等情况。

---

## 3. 用 $ \alpha $ 和 $ \beta $ 重新表达 E 步核心统计量（巩固）

有了前后向概率，之前提到的 $ \gamma $ 和 $ \xi $ 可以更完整地写为：

### (1) 单点后验 $ \gamma_t(i) $
$$
\gamma_t(i) = P(s_t=i | O, \lambda) = \frac{\alpha_t(i) \beta_t(i)}{\sum_{k=1}^{N} \alpha_T(k)}
$$
（分母即 $ P(O) $）

---

### (2) 相邻状态对后验 $ \xi_t(i,j) $
$$
\xi_t(i,j) = P(s_t=i, s_{t+1}=j | O, \lambda)
= \frac{\alpha_t(i) \cdot a_{ij} \cdot b_j(o_{t+1}) \cdot \beta_{t+1}(j)}{\sum_{k=1}^{N} \alpha_T(k)}
$$

可以看到，**$ \alpha $ 负责前向信息，$ \beta $ 负责后向信息**，两者在分母归一化后组合成完整的后验分布。

---

## 4. 完整 E 步计算流程（含前后向）

给定当前参数 $ \lambda^{old} $：

1. **Forward**：逐 t 计算并存储 $ \alpha_t(i) $，得到 $ P(O) $
2. **Backward**：逐 t 倒推计算并存储 $ \beta_t(i) $
3. 用上面公式计算所有 $ \gamma_t(i) $ 和 $ \xi_t(i,j) $

---

## 5. 为什么要单独强调前后向公式（目的再阐释）

- **前向**解决的是“到当前为止的似然”问题，它是递推的，避免了枚举所有状态序列（指数爆炸）。
- **后向**解决的是“未来观测的条件概率”问题，它与前向一起，通过乘法得到**完整序列下的状态后验**，这是 EM 能“软计数”的基础。
- 在 BIO 分词中，前后向让模型能够考虑**整句上下文**来判断每个字的标签概率（例如：句首的字更可能是 B，句尾不太可能是 I），而不是只看局部。

---

## 6. 补充：数值稳定性（实际实现中的小技巧）

由于概率连乘极易下溢，实际代码中通常使用 **对数域** 或 **缩放因子**（scaling）来递推前后向。缩放后的 $ \hat{\alpha} $ 和 $ \hat{\beta} $ 在计算 $ \gamma, \xi $ 时会自动抵消缩放因子，不影响最终结果。
