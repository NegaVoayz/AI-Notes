# Lecture 4

---

#### **1. 绪论：知识工程与专家系统**

- **核心公式类比**：
    - **程序 = 算法 + 数据结构** (传统计算)
    - **专家系统 = 知识 + 推理** (AI系统)
- **三大研究领域**：知识表示、知识获取、知识使用。其中，**知识表示**是后两者的基础和出发点。
- **专家系统结构**（延伸）：通常包括**知识库**（存储领域知识）、**推理引擎**（利用知识进行推理）、**解释器**（向用户解释推理过程）和**用户界面**。

---

#### **2. 核心知识表示方法详解**

讲义中列举了多种方法，以下是每种方法的精要总结：

| 方法           | 核心概念                                                     | 关键要素/结构                                                | 主要特点与用途                                               |
| :------------- | :----------------------------------------------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| **语义网络**   | 用**节点**表示实体/概念，用**有向弧**表示关系。              | - **关系类型**：`ISA` (继承), `AKO` (A Kind Of), `CAN-FLY`, `COLOR` 等。<br>- **推理**：通过**关系链**进行继承推理（如：Tweety → ISA → Canary → ISA → Bird → CAN-FLY → Fly）。 | 直观展示分类和属性继承，易于理解，但推理能力有限。           |
| **框架**       | 描述**对象或概念**的固定数据结构，包含多个**槽**。           | - **槽 (Slot)**：对象的一个属性。<br>- **侧面 (Facet)**：对槽的附加描述，如默认值、取值范围、过程等。 | 支持**默认推理**和**继承**。当信息缺失时，可使用框架的默认值。适合表示典型情况。 |
| **概念图**     | 基于逻辑的图形化表示，由**概念节点**和**概念关系节点**组成。 | - **概念**：可以是具体的（如：狗Emma）或抽象的（如：狗）。<br>- **关系**：二元关系（如：`COLOR`）或一元关系（如：`FILES`）。<br>- **量词**：使用**标记符（Marker）** 来指代特定个体。 | 表达能力接近一阶逻辑。可执行精确的图形化推理。               |
| **产生式规则** | 用“**IF-THEN**”规则表示知识。                                | - **前件 (IF)**：条件部分。<br>- **后件 (THEN)**：结论或行动部分。 | 模块化好，易于理解和修改。是专家系统的核心推理方式之一。     |
| **三元组**     | 知识图谱的基本表示形式。                                     | - **(主体, 谓词, 客体)**，如 `(sean_penn, rdf:type, Person)`。 | 是构建大规模知识图谱（如Wikidata, DBpedia）的基础。支持图查询。 |
| **脚本**       | 描述**特定场景**下事件发生的典型序列。                       | 由**进入条件**、**角色**、**道具**、**场景**和**结果**等组成。 | 用于自然语言理解和故事预测，属于**框架**的一种扩展。         |
| **其他方法**   | 讲义还提及：                                                 | 命题逻辑、一阶逻辑、描述逻辑、模态逻辑、决策表、知识Petri网、贝叶斯网络、神经网络、神经规则等。 | 每种方法适用于不同的问题类型和领域。                         |

---

#### **3. 关键概念与操作深度解析**

- **语义网络中的推理**
    - **继承**：子类自动继承父类的所有属性。这是语义网络最核心的推理形式。
    - **示例**：从“Sylvester owns a Canary”和“Canary is a Bird”可以推出“Sylvester owns a Bird”。

- **框架中的槽类型（讲义第7页）**
    1.  **标识信息**：框架的唯一ID或名称。
    2.  **与其他框架的关系**：如 `ISA` 和 `AKO`，用于构建继承层次。
    3.  **需求描述**：槽必须满足的条件或约束。
    4.  **过程信息**：与槽相关的程序或计算方法（如计算面积的过程）。
    5.  **默认信息**：当没有具体值时使用的默认值。
    6.  **新实例信息**：创建新实例时填写的具体数据。

- **概念图中的三种推理操作（重点）**
    1.  **限制（Restriction）**：将一个**一般概念**替换为一个**更具体的概念或个体**。
        - *例*：将概念 `[Dog]` 限制为具体个体 `[Dog: Emma]`，前提是 `Emma` 是这个语境下的一个已知标识符。
    2.  **连接（Join）**：将两个概念图中**相同的概念节点**合并为一个，从而整合信息。
        - *例*：图1有 `[Dog: Emma] → (CHASE) → [Cat]`，图2有 `[Dog: Emma] → (COLOR) → [Brown]`，连接后可得到包含Emma追赶猫且是棕色这两个信息的新图。
    3.  **简化（Simplify）**：删除概念图中**冗余或不必要**的信息，使图更简洁。
        - *例*：如果图中有 `[Dog: Emma] → (ISA) → [Dog]`，而 `[Dog] → (ISA) → [Animal]`，且无其他用途，则可以简化直接使 `Emma` 指向 `Animal`。

- **概念图中的标记符（Maker）**
    - **独特标记符（Unique Marker）**：如 `#Emma`，代表一个**特定的、唯一的**个体。
    - **通用标记符（Generic Marker）**：如 `*x`，代表一个**存在但不指定**的个体。在“**狗用爪子挠耳朵**”这个例子中，使用通用标记符表示“存在一只狗和它的爪子，它们之间存在挠的关系”。

- **命题概念（Propositional Concept）**
    - 指一个概念节点本身是一个完整的**命题或事件**。例如：“**Tom believes that Jane likes pizza.**” 中，“Jane likes pizza” 整个作为一个命题，是Tom相信的对象。

---

#### **4. 重要延伸知识点（连接讲义内外）**

- **逻辑基础**：
    - **命题逻辑**：以原子命题为最小单位，通过连接词（与、或、非、蕴含）组合。
    - **一阶逻辑 (FOL)**：引入了**量词（∀, ∃）** 和**谓词**，能够表示对象、属性和关系，表达能力远强于命题逻辑。概念图是FOL的图形化表示。
    - **描述逻辑 (DL)**：是FOL的一个子集，强调**分类（TBox）** 和**实例（ABox）**，是OWL（网络本体语言）的基础。
    - **模态逻辑**：引入“必然”和“可能”等模态词，用于处理知识和信念。

- **产生式系统与推理**（专家系统核心）：
    - **前向推理（Forward Chaining / 数据驱动）**：从已知事实出发，匹配规则前件，推出新事实，直到达到目标。
    - **后向推理（Backward Chaining / 目标驱动）**：从目标（假设）出发，反向寻找能支持该目标的规则，再验证规则的前件是否成立。

- **不确定性推理**（用于处理不完全可靠的知识）：
    - **贝叶斯网络**：用有向无环图表示变量间的概率依赖关系，可进行概率推理（如计算后验概率）。
    - **可信度方法（Certainty Factor）**：专家系统中常用，为每个结论赋予一个介于-1和1之间的可信度因子。

- **知识图谱 (Knowledge Graph)**：
    - 是一种基于**三元组**的大规模语义网络。
    - 查询语言主要是 **SPARQL**，用于从RDF（资源描述框架）数据中提取信息。

- **其他提及方法简述**：
    - **决策表**：用表格形式表示复杂的条件组合与相应行动。
    - **知识Petri网**：结合Petri网和知识表示，用于描述并发和同步过程。
    - **脚本（Script）**：用于表示固定场景（如餐厅就餐、看电影）的事件序列。
    - **神经规则（Neurules）**：结合神经网络学习和符号规则表示的方法。

---

#### **5. 典型问题与概念辨析（供理解查阅）**

- **语义网络 vs. 框架 vs. 概念图**：
    - **语义网络**：强调关系推理和继承，结构自由。
    - **框架**：强调对象的结构化描述，支持默认值，是“填表”式的。
    - **概念图**：逻辑基础最坚实，支持量词和复杂的逻辑操作（如连接、限制），推理更严密。

- **标记符的作用**：
    - 用于在概念图中明确一个概念是**具体的某个个体**（独特标记）还是**泛指某个未知个体**（通用标记）。这是概念图能表达丰富逻辑的关键。

- **槽（Slot）与侧面（Facet）**：
    - **槽**是框架的一个属性（如“颜色”）。
    - **侧面**是这个属性的具体描述（如“默认值是红色”、“取值范围是RGB”）。
